# Manifest schema migration

`brief/site-package/schemas/manifest.schema.json` is the public structural
contract for `submissions/*/*/manifest.json`.

## What changed

- `0.1.x` remains the legacy-compatible line. Its `role` values must still be
  stable lower-case `snake_case` tokens, but old omissions are reported as
  advisory findings instead of retroactively invalidating merged packages.
- `0.2.x` is the forward contract generated by `scaffold_ai_submission.py`.
  Every file entry has `path`, `role`, and a boolean `required` field. A role is
  extensible, but it must be a lower-case `snake_case` token no longer than 64
  characters. Canonical roles are documented in the schema description.
- `validation_claim` accepts additive metadata so a useful evidence note does
  not become an undocumented hard failure. Unknown top-level manifest fields
  remain invalid.

## Gate behavior

The pull-request validator executes the published schema for every hydrated
package. A legacy `0.1.x` mismatch is printed as a visible `legacy advisory`;
it is not silently ignored. The check becomes blocking when:

- the manifest declares schema `0.2.x`.
- a new package adds `manifest.json` without opting into `0.2.x`.

The opt-in is deliberate: an existing `0.1.x` package can still receive a
reviewed legacy revision, but its schema findings remain visible and advisory.
New scaffolds use `schema_version: "0.2.0"` and therefore fail closed.

This migration boundary avoids breaking already merged packages while making
new manifests fail closed. The existing file-existence and SHA-256
checks remain separate and continue to apply.

## Reproduce the corpus audit

From the repository root:

```bash
python3 -m pip install -r requirements-validation.txt
python3 scripts/validate_manifest_schema.py --all --json
```

Use `--strict` for a deliberate blocking audit. The default corpus audit is
advisory because historical packages are not rewritten by this change.
