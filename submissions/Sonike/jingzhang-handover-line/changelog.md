# 方案迭代记录

## v1.15 - 2026-08-14

本轮只做一件事：**把图面上的误导性精度去掉**。不新增任何主张、数据、来源或图件；几何、指标与三个矩阵未触及，`metrics.json` 一个数都没动。

- **缺陷。** `key-areas` 图上最醒目的三个数字是 `1,929,201.877 m²` / `1,043,236.909 m²` / `720,454.219 m²`，A0 `B/01` 上另有三座交接场与三期的同款写法，共 9 处。**这些面积算在临时边界上**——卡片自己在底部标着 `PROVISIONAL KEY AREA`——却给到 0.001 m²（1 平方毫米）的分辨率。评审 prompt 第 6 条明确要求检查 `misleading precision`，这是正撞在枪口上的一处。
  - **而且是包内自相不一致**：同一套图件里的 `metrics-evidence` 早就做对了——主数字写 `11.413 km²`，把 `11,412,825.386 m²` 降为小字注；`sheet_a0_b2.py` 的统筹/总体范围也一直是硬编码的 `43.6 km²` / `11.4 km²`。**所以本轮不是引入新写法，是把已有的正确写法统一到剩下 9 处。**

- **修法。** 图面复算面积一律按 **km² 表示到 0.01**（= 1 公顷），图注同步改为「面积按平方公里表示到 0.01，精确复算值见 metrics.json」。`_fmt_sqm` 的实现集中在 `render_figures.py`、`sheet_a0_b1/b2/b3.py` 四处，改的是格式化函数而非各处字面量。

- **正文的精确值不动，这是有意的。** 正文写着「按临时几何在 EPSG:4548 中复算的总体范围为 11,412,825.386 平方米；**保留小数是为了让第三方能用同一几何复现同一结果，不代表该数值具有官方精度**」——它给了精确值、说明了目的、划了边界，是可复算性的举证。**图面与正文的要求本来就不同**：图面是视觉印象，一个没有上下文的毫米级数字摆在最醒目处就是误导性精度；正文是证据，带解释的精确值是溯源链的一环。把正文也改掉反而会削弱「指标可独立复算」这条主张。

- **顺带修掉一处缺字符方块。** 新写的 A0 图注最初用了 `km²`，但那句注记走中文字体（`NotoSerifSC` 子集）渲染，子集里没有上标 `²`（U+00B2），实测渲染为 `km☒`。改用中文「平方公里」避开该码位。**同页的 `1.93 km²` 没有这个问题**——它走的是 `Helvetica-BoldOblique`（PDF 基础十四款之一，WinAnsi 编码含 `²`）。这一条记下来：**同一页里中文字体与拉丁 base14 的可用字符集不同，跨字体复用同一个字符串会踩雷。**

- **构建按载体分三条路径，各有核验口径。**
  1. **5 张核心图件**由生成器重出，与上一版逐像素比对：差异**只落在页脚版本印记带**（`y=962–972`），`key-areas` 另有面积数字行（`y=690–710`），**无其他图面变化**——脚本未漂移。
  2. **其余 8 张图件**（16 个文件）走重印：14 张 PNG 声明矩形外**逐像素零变化**，2 张 JPEG 沿用原量化表重新编码，矩形外均值偏差 0.043。
  3. **四套 PDF**：A0 前 3 页因面积改写而重建，新页**各自单独**经 Ghostscript 子集嵌入 Noto Serif SC——不整份重蒸馏，避免把保留页的内嵌图件重新压缩一遍；A3 **全部 13 页保留原页**（A3 页内没有毫米级面积，`sheet_a3_02.py` 用的是硬编码 km²），只按灰度指纹替换内嵌图件、按等长替换刷页脚。
  - **A3 首页特别说明**：`sheet_a3_01.py` 仍停留在已被撤回的「任务书对照页」版本，本轮**刻意不用它重建首页**，A3 首页因此与撤回后的开篇页逐像素一致。

- **核验。** 页数与 `PACKAGE v1.15` 命中数逐套相等（13/13、13/13、6/6、6/6），`PACKAGE v1.14` 残留逐套为 **0**，`qpdf --check` 四套 PASS，`pdffonts` 四套仍为 `<子集前缀>+NotoSerifSC / CID TrueType / UniGB-UCS2-H / emb=yes`，视觉输入仍为 18 张。

- **把构建工艺日志从 `proposal.md` 移出（2026-08-14 追加，不改版本号）。** 正文「风险、版权与合规说明」一章此前 7,666 字、占全文 15.7%，是全篇最大的一章——比任何一个设计章节都大。其中约 1,950 字并不是合规内容，而是**版本迭代日志**：mobility-bluegreen 小注被切的修复经过、A0 标注碰撞与对比度实测、图纸前三页由矢量重排、编号体系冲突消除、逐版重建路径与逐像素比对结果。
  - **这些内容属于 changelog，不属于 proposal.md。** 评审评的是城市设计方案，不是我方的构建工艺史；而且同样的内容在本文件里本就逐版完整记录，移出不丢失任何信息。正文只保留可核验的结论（全包一个版本号、页脚一致性口径、`pdffonts` 结果），并注明具体路径见 `changelog.md`。
  - **合规实质一条未删**：权利明细表（四类资产 × 权利依据 × 核验命令）、`COMMUNITY-DISPLAY-ONLY` 自述含义、26 张图件无生成式素材、渲染脚本未随包提交因而只主张「指标可复算」不主张「逐字节可复现」——全部原样保留。删的是叙述，不是证据。
  - **顺带修掉一处 v1.15 遗留的不一致**：正文写着重点区卡片「标注 metrics.json 的复算面积（1,929,201.877 / 1,043,236.909 / 720,454.219 m²）」，但 v1.15 已把图面改为 km²，该句已随之更新。
  - `[source:FONT-ADOBE-CID]` 的引用随被删段落一并消失，触发确定性校验的 `missing source reference`；该来源仍然成立（PDF 编码确实沿用 `UniGB-UCS2-H` 与 Adobe-GB1），故在保留的字体段落中恢复引用，而不是删除来源。
  - 中文正文 48,099 → 46,145 字，英文 109,888 → 104,655 字；「风险、版权与合规说明」由 15.7% 降至 12.2%。

- **把双联交接账协议实例化到全部 12 个场景，并做缺陷注入检验（2026-08-14 追加，不改版本号）。** 此前 `schema_valid_synthetic_shift_count = 1`——协议结构完整，但只有 SCN-05 一条实例。**一套只跑通一个例子的规则，看不出它在别的场景是否也成立**，协议因此无法被逐场景核验。
  - **12 条实例全部由包内已有数据推出，未新增设计主张。** 每条交接账的字段都来自 `geometry/public_space.geojson` 已登记的场景属性：`human_fallback` → 人工服务底线渠道、`exit_condition` → 停机触发与回滚演练夹具、`accountable_role` → 服务责任方、`rollback_state` → 回滚目标状态、`applicability_scope` → 阻塞性未决项、`next_review_trigger` → 复议触发器。
  - **唯一的设计决策是双联角色对的拆分**，且逐条写明依据。协议要求交出与接入由不同角色判断，而部分场景的 `accountable_role` 只写了一方：SCN-02/04/05/07 的 geojson 已写明两方，直接采用；其余按该场景 `exit_condition` 指认接入方——例如 SCN-09 社区照护排班桌的首要停用条件是「当事人拒绝算法分配」，接入方因此是**照护当事人代表**而非服务方；SCN-10 城市夜班安全灯含「隐私边界被突破」，接入方是隐私与安全复核者而非照明维护方。
  - **七条规则做了缺陷注入。** 规则不是新写的，是把已有约束显式化：R1 双联分离、R2 人工底线先行、R3 阻塞未决项保持智能层关闭、R4 回滚未观察不得试用、R5 合成不触真实、R6 拒收须给理由、R7 未指派不得签认。对每条交接账先跑基线、再逐条注入对应缺陷，共 **96 条**（12 基线 + 12 场景 × 7 规则）。
  - **结果**：schema 校验 12 条 **0 错误**；基线 **0 条**触发规则（协议不误拦合规实例）；84 条注入 **全部被拦截**，漏检 **0**。逐条结果见 `visual/assets/governance/rule-check-report.json`，实例见 `shift-ledger-suite.json`。
  - **生成过程中 schema 真实拦下了四类不合规写法**，说明校验不是走过场：`channel_id` 必须匹配 `^HF-[0-9]{2}$`（原写 `HF-07-02`）、`item_id` 必须匹配 `^OPEN-[0-9]{2}$`（原写 `UR-11-01`）、`drill_mode` 必须是 `synthetic_tabletop`、`use_notice.classification` 必须是 `concept_protocol_fixture`。四类共 144 处错误逐轮修正到 0。
  - **只证明规则闭合，不证明现场绩效、安全、合规或获批。** 12 条实例的 `receiver_disposition` 一律 `refused`、`smart_layer_state_after_decision` 一律 `off`——三条验收门在任何场景都尚未满足。**协议在纸面上正确地拒绝了它自己的全部 12 个场景**，这正是它应有的行为：先证明拒收路径可靠，再谈放行。
  - 指标：`schema_valid_synthetic_shift_count` 1 → **12**；新增 `shift_protocol_rule_check_count = 96` 与 `shift_protocol_rule_check_miss_count = 0`；两个新文件登记进 manifest（63 → 66 条）。几何、三个矩阵、26 张图件与四套 PDF 均未触及，故不改版本号。

## v1.14 - 2026-08-11

本轮只做一件事：**修掉 `mobility-bluegreen` 底部小注被卡片下边框切掉的问题**。不新增任何主张、数据、来源或图件；几何、指标与三个矩阵未触及。

- **缺陷。** 图件底部的「沿线定位示意，非道路红线」/「Locator sketch, not a road redline」下沿被卡片边框切断。几何上很清楚：地图块底边 827 px、小注顶边 881 px、13 px 字行高约 18 px，底端落到约 899 px，而 `card_y1 = 895`。中英两版由同一生成器产出，**因此都被切**。这张图是评审直接看到的五张图件之一，中英两版都在视觉输入内。

- **修法。** 地图块高度由 560 px 收到 545 px 让出空间。没有选择压缩比例尺与小注之间的间距——比例尺标签绘制在其基线上方 18 px，与地图块底边只剩 12 px，再压会造成新的碰撞。

- **这一处是逐张普查找出来的。** 本轮把评审实际收到的 18 张视觉输入过了一遍：五张核心图件的中英两版、四套 PDF 首页、四张离线页面截图。**只发现这一处**。`key-areas.en.png` 的「Along the line」到卡片边框余量 6 px（中文 28 px），偏紧但实测并未重叠，判定不是缺陷，未作改动——**放大图上看着像切字，是抗锯齿造成的错觉，量过才排除**。

- **重刷与核验。** `mobility-bluegreen` 中英两版按 v1.14 重新生成；其余 24 张图件重刷版本印记，声明矩形外逐像素零变化（2 张 JPEG 沿用原量化表，矩形外均值偏差 0.05）。四套 PDF 的页脚与内嵌图件等长替换，页数与 `PACKAGE v1.14` 命中数逐套相等（13/13、13/13、6/6、6/6），无旧版本号残留，`qpdf --check` 四套 PASS。

- **A3 首页重建为任务书对照页（2026-08-13 追加，不改版本号）。** 评审脚本 `collect_visual_inputs()` 对每套 PDF 只送**第 1 页**（`page_limit=1`，函数签名的默认值 3 未被使用），因此 `a3-booklet` 的 `JZ/01` 单独承担该套图纸的全部证明力，第 2 页起不进评审。原 `JZ/01` 是开篇页：一张细长总平面 + HANDOVER PROTOCOL 四条 + 一句结语，而这三部分**都已被 `a0-boards` 的 `B/01` 以更详细的形式覆盖**，等于把两张视觉额度花在重复信息上。本版把 `JZ/01` 右栏换成**三大定位**、底部结语区换成**五大功能 × 空间对应**（文案与 `JZ/03` 及 `proposal.md` 任务书回应表逐字一致，不新增主张）；左栏地图保留并由 0.45 收窄至 0.36 栏宽——场地是南北细长条，按比例缩放后原栏宽用不满。
  - **顺带修掉两处英文排版缺陷**：图例第三项 `Handover yard node` 曾越过卡片右边框被切字，改为字号固定 10.5pt、放不下就折行（`legend_h` 随行数联动，否则第二行会压到地图区）；底部来源注记曾把 `design only` 硬切成 `desig` / `n only`，因为共用的 `wrap_cn` 是逐字断行——本页改用按单词断行，未改动 `wrap_cn` 本身以免影响其他页。
  - **字体与其余页同形**：reportlab 只能排出 `STSong-Light`（CID / UniGB-UCS2-H / `emb=no`），若直接拼入，同一 PDF 内会出现两种中文字体、其中一种未嵌入，正文那条以 `pdffonts` 为核验命令的声明即变成不实陈述。故沿用 v1.11 工艺：排版后经 Ghostscript + `cidfmap` 换入并子集嵌入 Noto Serif SC（OFL-1.1），再改 `BaseFont` 名避免自述与实嵌不一致。结果为 `<子集前缀>+NotoSerifSC / CID TrueType / UniGB-UCS2-H / emb=yes`，与第 2–13 页同形。
  - **核验**：两套第 2–13 页 100 dpi 渲染逐页比对**全部逐像素相同**；页数与 `PACKAGE v1.14` 命中数均为 13/13；`qpdf --check` 两套 PASS；视觉输入仍是 18 张，未新增或缺失任何一张。
  - **已知未修**：四套 PDF 的子集字体 `ToUnicode` 映射有误，`pdftotext` 提取正文为乱码（英文为逐字符 −1 偏移），页脚 `PACKAGE v1.14` 正常。这是 Ghostscript 蒸馏工艺的既有问题，新页与其余页表现一致，本轮未引入也未修复；对评审无影响（评审读渲染图），但影响公开发布后的文本复制与屏幕阅读器，待下一轮处理。

- **权利表述订正（2026-08-13 追加，不改版本号）。** 上面这一版的正文概括句写作"唯一使用的第三方字体是 OFL-1.1 授权的 Noto Serif SC"，而同一节的明细表第三行与 `sources.json` 的 `FONT-STHEITI-RASTER` 都记着第二款字体——栅格图件的文字由 macOS 系统字体 STHeiti 渲染。**披露一直是全的，错的是那句概括：它把"包内唯一被再分发的字体"写成了"唯一使用的字体"，于是概括句与自己的明细表相互矛盾。**本次把中英正文的该句改为分别说明两款字体各自承担的义务（PDF 子集嵌入 vs 本机栅格化、包内不含字体文件故不构成再分发），并把明细表那一行的资产类别由"PNG 图件"扩为"栅格图件（24 张 PNG 与 2 张 JPEG）"，以覆盖同样由 Pillow 渲染的 2 张 JPEG，与 `sources.json` 中该来源"仅用于栅格图件"的措辞对齐。**只改这三处文字与由其渲染的两个 HTML；几何、指标、来源、三个矩阵、26 张图件与四套 PDF 均未触及，故全包版本印记仍为 `PACKAGE v1.14`，不制造跨载体的版本号并存。**

- **撤回上述「A3 首页重建为任务书对照页」，`JZ/01` 恢复为开篇页（2026-08-14，不改版本号）。** 该次改版按下述推理做出：评审只送每套 PDF 的第 1 页，故 `JZ/01` 应尽量多承载任务书对应信息。**这个推理的前提是错的**——评审收到的是一张 **72 dpi 的 A3 图面**，在该分辨率下评的是图面表达质量；而任务书回应的文字内容在 `proposal.md` 的对照表里本就完整可读，图上复述一遍并不增加信息，却把这套图纸唯一的视觉额度从「设计表达」换成了「行政对照表」。改版后右栏塞进三大定位加五大功能共 8 个小字卡片，地图栏宽由 0.45 收到 0.36，留白与底部宣言一并消失。
  - **还破坏了跨载体的视觉一致性**：`visual/index.html` 首屏内嵌的 `F/01` 缩略图、以及 `a0-boards` 的 `B/01`，用的都是「总平面 + HANDOVER PROTOCOL 四条」这一套开篇语言。把 A3 首页单独换掉，等于让同一方案在三个载体上给出两种开篇。原判断说这三部分「已被 `B/01` 覆盖、属于重复」——**同一视觉标识在不同载体上重现是一致性，不是冗余**。
  - **撤回方式**：`drawings/a3-booklet.pdf` 与 `.en.pdf` 整文件恢复为改版前的字节，因此该次改版顺带修的两处英文排版缺陷（图例第三项折行、来源注记按单词断行）也一并回到改版前状态，**如实记录，不假装只撤了一半**；这两处将在下一轮以不动版式的方式单独处理。正文「构建溯源」段落中为该次局部重建而改写的口径同步恢复为原文，因为撤回后全包重新等同于 v1.14 的整体构建产物。
  - **保留**：同日的「权利表述订正」不在撤回范围内，正文两款字体的分别说明与明细表的「栅格图件（24 张 PNG 与 2 张 JPEG）」维持不变——那一处修的是真实的自相矛盾，与图面无关。
  - **核验**：`a3-booklet` 两套页数与 `PACKAGE v1.14` 命中数均为 13/13，`qpdf --check` 两套 PASS，视觉输入仍为 18 张；全包版本印记仍是 `PACKAGE v1.14`，未制造跨载体的版本号并存。

- **补登记栅格图件的拉丁字形来源，并把三条字体来源的适用范围收窄到各自成立的范围（2026-08-14，不改版本号）。** 上一条修的是「概括句与自己的明细表打架」，这一条修的是同类问题里更实质的一种：**明细表本身漏了一款实际在用的专有字体**。
  - **缺口。** 生成图件的 `jzstyle.py` 里 `font_latin()` 加载的是 `/System/Library/Fonts/Helvetica.ttc`，26 张栅格图件的**页脚版本图签、左上角图号、指北针字母与比例尺数字**都由它栅格化。但正文明细表把「栅格图件中的文字」整行归给 STHeiti，`sources.json` 的 `FONT-STHEITI-RASTER` 也写作「全部 PNG 图件的文字」——**这两处陈述都不准确，图件里的拉丁字形另有来源**。
  - **更麻烦的是同名不同性质。** 明细表第一行「图纸中的拉丁文字 | Helvetica 家族」写的是 PDF 规范的基础十四款字体：按字体名引用，全程不碰任何字体文件，故不承载授权义务。图件里那款虽然同叫 Helvetica，却是**真的加载了随 macOS 分发的字体文件**并用其字形生成像素，性质与 STHeiti 相同。原表述容易让读者以为第一行已经把「拉丁文字」全覆盖了，实际上它只覆盖 PDF。
  - **修法。** `sources.json` 新增 `FONT-HELVETICA-RASTER`（22 → **23 条**），登记加载路径、取用的字面（`.ttc` 第 3 号 Bold Oblique 用于图签与图号）、许可性质与限制；`FONT-PDF-BASE14` 的 `usage` 与 `usable_for` 收窄到「四套 PDF」并在 `not_usable_for` 明确排除图件；`FONT-STHEITI-RASTER` 改为只覆盖中文字形；`FONT-NOTO-SERIF-SC` 的交叉指向按载体拆开。正文概括句由「两款第三方中文字体」改为三款需分别说明义务的字体加一种 PDF 按名引用的情形，明细表拆出「栅格图件中的中文文字」与「栅格图件中的拉丁文字与数字」两行，中英对称。
  - **不夸大可核验性。** 栅格产物无法反查字形来源，新增那一行的「已知限制」如实写明：本行属渲染阶段自述，可独立核验的只有「包内不含任何字体文件」这一半（`find ... -name '*.tt[cf]'` 无结果）。
  - **计数同步**：正文「二十二条来源」→ 二十三条、「其余 15 条不在中央表内」→ 16 条、「字体与构建工具链五条标 `not_a_factual_source`」→ 六条，均按 `sources.json` 实测值改写，中英对称。
  - **不改版本号**：只动文字与 `sources.json`，几何、指标、三个矩阵、26 张图件与四套 PDF 均未触及。

## v1.13 - 2026-08-11

本轮只做一件事：**修掉 A0 首版面上一处标注与线条的碰撞，并把该处文字提到 WCAG AA 对比度**。不新增任何主张、数据、来源或图件；几何、指标与三个矩阵未触及。

- **缺陷。** A0 `B/01` 左侧总平面上三个交接场的标注直接落在地图上。流向文字的基线设在节点中心下方 12pt，而 21pt 字的大写高约 15pt，正好跨过节点所在的横向缝合支线——`BUILD→VERIFY` 的前几个字母被引线和节点圆点横穿；`SHARE→SERVE` 是浅黄字压在黄色地块上，几乎不可读。这一页在评审的视觉输入内（每套 PDF 取首页、72 dpi），缺陷因此直接暴露在评分视野里。

- **修法沿用工程内已有的正确做法。** A3 `JZ/01` 早已用「引线 + 底衬卡片」处理同一问题，代码注释写明「完全遮住其下线条，杜绝标签与线条/图形碰撞」。本轮把该模式按 A0/A3 = 2.003 的比例移植到 `B/01`：引线止于卡片边缘，卡片完全遮盖其下线条与色块，节点圆点最后绘制。**问题不在于没有解法，而在于同一工程里两页用了两套做法。**

- **对比度按实测调整，不按目视。** 三个节点色直接落在卡面（#faf7ef）上的 WCAG 对比度实测为红 3.72:1、青 2.79:1、黄 1.50:1，**全部低于 AA 的 4.5:1**。卡片内的流向文字改用同色相压暗值 #D4301F / #007E79 / #8B6C0B（实测 4.62 / 4.60 / 4.62:1），达标且保留颜色编码；地图上的节点色点仍用原色，不影响图面语言。**此前只有黄色被肉眼看出问题，红与青是量过才发现同样不达标**——这一条记下来，以后不要用目视代替测量。

- **重建范围与核验。** 重新生成 A0 六页（zh/en × B/01–B/03），与包内保留页拼装后经 Ghostscript 嵌入 OFL 字体并订正字体名；四套 PDF 页数与 `PACKAGE v1.13` 命中数逐套相等（13/13、13/13、6/6、6/6），无旧版本号残留，`qpdf --check` 四套 PASS。26 张图件重刷 v1.13：24 张 PNG 声明矩形外**逐像素零变化**，2 张 JPEG 沿用原量化表（矩形外最大偏差 11/12、均值 0.05）。独立图件 `site-overview.png` 本就无此缺陷（标注在图外），未作改动。

## v1.12 - 2026-08-10

本轮只做一件事：**让 `sources.json` 说中央登记表的原生词汇，使来源状态可以被机器逐项对照**。不新增任何来源、主张、数据或图件；几何、指标与三个矩阵未触及。

- **每条来源新增与 `data/source_registry.json` 的对照。** `usable_for_formal` 改用中央表同一套取值词汇。其中 **7 条**确实在中央表内（临时范围几何、重点区几何、征集公告、智能体任务书、生成式AI暂行办法、无障碍环境建设法、国办发〔2020〕45号），其 `registry_source_id`、`registry_authority_level`、`registry_review_status`、`usable_for_formal` 四项由脚本从中央表**逐字复制**、未经改写，标 `usable_for_formal_basis: central_registry_verbatim`。

- **未登记的 15 条如实标注，不借机上调状态。** 一律标 `registry_review_status: not_in_central_registry` 与 `usable_for_formal_basis: participant_self_assessed`。维护者已裁定参赛者自采源不必重复登记中央表、投稿包的 `sources.json` 才是完整记录；**但自评状态不等于中央批准**，本包不据此主张任何中央审定结论。这条边界是刻意写死的——v1.10 正是栽在把自设标准写成法定义务这类越界表述上。

- **字体与构建工具链 5 条单列 `not_a_factual_source`。** 它们是资产与软件许可记录，不是事实来源，不支撑任何设计结论，塞进 `background_only` 会是错误分类。

- 订正正文两处过时计数（「二十一条来源」→ 二十二条），中英文对称；`self_check.json` 的 `SOURCE_PROVENANCE` 同步。

- **`self_check.json` 迁移到 `persisted-self-check-v1` 契约。** 按 PR #1521 的复核意见执行 `self_check_submission.py --mark-self-checked`：持久化顶层 `ok=true`、`can_enter_formal_review=true`、`review_status=formal-review-ready` 与四条规范化阻塞门（`DETERMINISTIC_VALIDATION` / `SPATIAL_REVIEW` / `VISUAL_PACKAGING` / `PROFESSIONAL_EVIDENCE`），`manifest.validation_claim` 增 `readiness_contract: persisted-self-check-v1`。

  **两类记录并存，且不可混淆。** 该工具按设计只保留 `schema_version`、把 `checks` 整体替换为四条门禁，本包原有 29 条自撰证据条目会全部消失。`self_check.schema.json` 顶层是 `additionalProperties: false`，无法另开字段存放，但 `checks.items` 允许附加属性，因此改为：**四条工具实跑记录在前并标 `record_type: trusted_gate_run`，28 条自撰断言在后并标 `record_type: participant_assertion`**（原第 29 条 `PROFESSIONAL_EVIDENCE` 与机器门同名，以实跑记录为准不予恢复，其原内容为概括句、正文已覆盖）。这样既不丢证据，也不会把自撰断言伪装成机器实跑结果——**四条门禁的内容一字未改，全部由工具写入**。

  另有 **2 条自撰条目的内容此前只存在于 `self_check.json`**，已一并补进正文（中英对称），避免只依赖单一载体：① 26 张图件全部由 PIL/reportlab 程序化绘制、无生成式图像素材（v1.6.4 起该路径承载的是程序化绘制的《交接断面》F/13）；② 三处重点区卡片按 `key_areas.geojson` 真实轮廓与同一比例尺绘制，附主轴、定位条、指北针、比例尺与复算面积。

- **版本印记整体重刷 v1.11 → v1.12。** 24 张 PNG 声明矩形外**逐像素零变化**；2 张 JPEG 沿用原量化表，矩形外最大通道偏差 14/15、均值 0.07；四套 PDF 的 38 处页脚与 20 张内嵌图件等长替换，`PACKAGE v1.12` 命中数与页数逐套相等（13/13、13/13、6/6、6/6），无旧版本号残留。

## v1.11 - 2026-08-10

本轮只做一件正事：**把四套 PDF 的中文字体换成许可可核验的开源字体并嵌入**，关闭一个由本包自己登记在案、却一直没有关闭的阅读依赖。附带订正两处过时表述、整体重刷版本印记。不新增任何主张、数据或图件，几何、指标与三个矩阵未触及。

- **中文字体从"按名引用、不嵌入"改为"子集嵌入"。** v1.10 及更早版本的四套 PDF 都是 `STSong-Light` CID、`emb=no`，权利与构建溯源表里我们自己写着"阅读器需自带中文字体包，否则中文可能不显示……正式出版前应改为嵌入获授权字体"。合并后的包会经 GitHub Pages 公开发布，这个依赖不应该继续留着。现改为嵌入 **Noto Serif SC Light**（SIL Open Font License 1.1，取自 noto-cjk 官方仓库的可变字重 TTF，sha256 `5326cfb0…`，用 fontTools 4.60.1 固定 wght=300 得静态实例）。选它的理由不是外观，而是 **OFL 是少数明文允许把字体子集嵌进文档并随文档再分发的许可**——嵌入这个动作本身有据可依，不必依赖"我们认为可以"。宋体对宋体，字形风格与原引用一致。

- **替换只改字体程序，不改版面。** 用 Ghostscript 10.07.0 经 `cidfmap` 指定字体程序重蒸馏，PDF 内容流沿用原有排版：四套的页数、页面尺寸、断行与字位逐项不变；内嵌图件用 `pdfimages` 提取后逐字节比对为**逐像素相同**；`qpdf --check` 四套全 PASS。四套合计 2.7 MB → 3.1 MB。

- **同时订正 PDF 内的字体名对象。** Ghostscript 会保留原 `BaseFont` 名，结果是文件自述 `STSong-Light`、实际嵌的却是 Noto 字形。这属于自相矛盾的溯源，已用 qpdf 把四套共 12 处 `/BaseFont`、`/FontName` 改为 `NotoSerifSC`，改名前后渲染逐像素相同。

- **拉丁文字仍不嵌入，并写明理由。** Helvetica 家族与 ZapfDingbats 属 PDF 规范要求阅读器自备的基础十四款字体，不构成显示风险；若只为让 `pdffonts` 全行显示 `emb=yes` 而嵌入某个仿制实现、却继续挂 Helvetica 之名，反而是更差的溯源。

- **证据层同步。** `sources.json` 新增 `FONT-NOTO-SERIF-SC`（URL、版本、两个 sha256、许可读自 name 表 nameID 13/14、可用与不可用边界），`FONT-ADOBE-CID` 由"字体来源"降为"编码声明"（仍使用 `UniGB-UCS2-H` 与 Adobe-GB1 字符集），`FONT-PDF-BASE14` 与 `TOOLCHAIN-BUILD` 相应更新——工具链新增 fontTools(MIT)、qpdf(Apache-2.0)、Ghostscript(AGPL-3.0)，并写明后者与 PyMuPDF 同属"仅在本机运行的工具"，未分发代码、未链接进交付物，嵌入的字体子集来自 OFL 字体而非任何 AGPL 组件。正文「权利与构建溯源」表与 `report/copyright_statement.md` 中英文对称更新。

- **订正一处过时事实：参赛者 PR 现在可以删除文件了。** 包内多处仍写着"路径保留而非删除，是因为参赛者 PR 无法删除文件（Issue #647，修复 PR #668/#671 未合并）"。实际 **#671 已合并**，`scripts/github_pr_validation.py` 的 `validation_paths_for()` 已无条件排除 `status == "removed"`。`handover-scene.jpg` 继续保留的理由改为唯一正确的那个：该文件承载的《交接断面》（F/13）本身就是成果的一部分。`report/copyright_statement.md` 中英两段与 `self_check.json` 一并订正。

- **版本印记整体重刷 v1.10 → v1.11，全包仍只有一个版本号。** 24 张 PNG 图件在声明矩形之外**逐像素零变化**（`v1.10` 与 `v1.11` 在等宽数字下图签宽度相同，矩形不扩大）；2 张 JPEG 沿用原图量化表重新编码，矩形外最大通道偏差 24/27、均值 0.08（不保留量化表时最大偏差为 152，故必须保留）；四套 PDF 的 38 处页脚与 20 张内嵌图件按等长替换写入，`pdftotext | grep -c "PACKAGE v1.11"` 的命中数与页数逐套相等（13/13、13/13、6/6、6/6）。

## v1.10 - 2026-08-09

本轮做四件事：**订正三处把自设标准写成法定义务的合规表述**、**把堆在段落里的证据标记改成可逐行对照的结构（一个标记也不删）**、**修正一处仅在英文版出现的图面重叠**、**把版本印记整体重刷为 v1.10**。不新增任何主张、数据或图件。

- **订正合规基线：把法定下限与本方案自设标准逐行分开。** 原表把三条红线统称为「各有法定或政策出处」，其中三处超出了来源登记允许的用途：① 将《生成式人工智能服务管理暂行办法》第十四条概括为「任何智能服务必须可被停止」——该条是**发现违法内容时的处置义务，未设定面向一般用户的停用权**；② 第十五条被表述为「必须便捷且有时限」，而该办法**只要求公布反馈时限、未规定具体数值**；③ 将《无障碍环境建设法》第三十九条概括为「公共服务必须保留人工办理」，而该条**仅适用于涉及医疗健康、社会保障、金融业务、生活缴费等事项的公共服务场所，不适用于所有公共空间或数字界面**。现改为四列结构，逐行写明「法定依据与其实际效果」「依据未覆盖、由本方案自设的部分」，并把表后「在既有法规下……本来就不具备进入公共空间的资格」改写为明确的设计主张。正文另有三处同类表述（风险表缓解措施、公平性下限段、参考资料条目）一并订正，中英文对称。**把自愿采用的设计标准写成普遍法定义务是一种误导，即使它使方案看起来更有依据。**

- **把同一处订正同步到机器来源层。** 上一条只改了正文，`sources.json` 的三条法规记录仍保留旧的过宽结论，会把机器审计带回被订正掉的口径——由 PR #1270 的独立回读指出。三条记录的 `usage` / `cross_check_note` / `usable_for` / `not_usable_for` 逐项收窄到与中央 `data/source_registry.json` 的 `allowed_uses` / `prohibited_uses` 一致：① `GENERATIVE-AI-INTERIM-MEASURES` 删去「三条共同支持停用按钮」与「论证接管亭的物理停用入口与投诉入口具有法定依据」，改为分列三条条款各自的适用对象，并把常设停用入口、数值时限、具体空间载体明确标为本方案自设；② `BARRIER-FREE-ENVIRONMENT-LAW` 删去「把无AI等价服务红线锚定为法定义务」与「为组件库、接管亭与城市交接厅设定下限」，限定于第三十九条列举的服务事项；③ `ELDERLY-SMART-TECH-PLAN` 的 `evidence_class` 由 `regulatory_baseline` 改为 `background_only`（与中央登记的 `usable_for_formal` 一致），并写明「并行」原则不等于「不得取消传统渠道」。`not_usable_for` 相应增加 9 条禁止推论。
- **同一口径的另外三处残留一并订正**，均在上一条的检查范围之外：概览表「合规锚点」行仍写「不是设计者的善意，而是对现行法规的响应」（中英文各一处）；合规基线表最后一行把国办发〔2020〕45号放在「法定依据」列且第三列填的是政策内容而非自设部分；`self_check.json` 的 `REGULATORY_BASELINE` 仍写「三条红线分别对应……第十四、十五、十七条」。三处均改为与合规基线表一致的分列表述。
- 起因是 CI 的 6 条非阻塞告警，全部指向 `proposal.md` 的证据标记密度。读 `scripts/validate_submission.py` 的 `reference_density_issues()` 后确认判定口径：先按空行切块，**若块内 ≥2 行且其中 ≥2 行以 `|`、`- `、`* `、`+ ` 或序号开头则逐行判定，否则整块判定**；每个判定单元标记数上限 8，最长连续串上限 3，而「连续」要求两标记间的分隔文本 ≤12 字符且只含空白与顿号类符号。
- 由此得出的改法不需要删标记：**把标记堆砌的散文改成表格或列表（每行挂一个），并在标记之间写清该标记指向什么**。后者顺带打断连续串，因为中文词不在分隔符字符集内。
- 七处命中逐一处理：设计依据段的五个来源改为「任务定义／任务分解／资料底板／使用边界／事实导航」逐项标注；计算依据段按几何与标准断为两段；用地与分期面积段改为两张表；标准矩阵索引拆为项目文件与国家规章两条并标明各管什么；六个国际案例各自写出机构名；九类几何图层改为逐行列表；十五项专业深度改为按专业维度排列的表。
- **核验：密度告警 7 处归零，证据标记总数改前改后同为 222 个，集合差为空**——没有任何一个引用键被删除、替换或新增。确定性校验的告警由 7 条降至 1 条，仅剩临时边界一条（评审口径明确不因组织方数据缺口扣分）。
- 附带修正两处可读性缺陷，均使用既有数据、不引入新事实：三处重点区此前在正文中只有三个裸面积数字，现按 `geometry/key_areas.geojson` 的 `name_zh` 标出「众智园AI自主创新加速区／北京AI原点社区／大钟寺AI产业聚集区」与各自几何标识；十五项专业深度此前是一串裸标记，现按 `design_depth_matrix.json` 的 `title_zh` 与 `professional_dimension` 列出中文名与专业维度。
- 一并订正一处过时自述。此前正文称「引用键写在正文而不是表格单元格里，是为了让离线 HTML 的表格不出现未渲染的原始标记」——该顾虑源于早期渲染缺陷，现已不成立：`render_proposal_html.py` 会把表格单元格内的标记同样渲染为证据条，改动前后离线 HTML 中的标记数均为 222，无一处以裸文本出现。该句已改写为说明两种写法的适用场合。
- **修正 `key-areas.en.png` 底部来源说明与版本图签的重叠。** 英文说明串比中文宽，末尾压进图签 29px，"professional teams" 词尾被吃掉；中文版同处间隙 141px，不受影响——这是仅在英文语种下越界的排版缺陷。缩短英文串后间隙恢复到 164px，五个信息点（范围来源／点位来源／同一比例尺／临时边界／仅供专业团队）全部保留。该图唯一的嵌入处 `drawings/a3-booklet.en.pdf` 同步更新：按裸 RGB 逐字节精确匹配替换单个图像对象，逐页复核确认 13 页中仅第 7 页变化、其余 12 页逐像素一致。包内其余 12 张英文图件底部间隙 246–440px，已批量筛查无同类问题。
- **版本印记整体重刷 v1.9 → v1.10，全包仍只有一个版本号。** 26 张图件、38 页图纸与两个离线页面的页脚、以及 `proposal.md` / `proposal.en.md` 的 `iteration` 与 `agent.json` 一并刷新。工艺沿用 v1.9：5 张核心图件由渲染脚本重新生成（重渲前先验证与包内现图逐像素一致，确认可安全重建），其余 21 张按声明矩形重印并断言矩形外零像素变化（14 张 PNG `outside_diff=0`，2 张 JPG 仅有 0.116 的重压缩损失）；图纸新页重新生成，保留页按内容流算子重排图签。复核：四套 PDF 共 38 页**每页恰好一处 `PACKAGE v1.10`，v1.9 与更旧格式零残留**；新图签比旧的宽 8px，已逐张验证 26 张图件底部说明与图签的间隙，最小 97px、无碰撞。
- **本轮未触及几何、指标与三个矩阵**，这些文件与 v1.9 逐字节一致。角色仍 `unassigned`、KPI/SLO 仍 `null`、仍无现场演练，本轮不增加任何执行性主张。

## v1.9 - 2026-08-09

本轮做两件事：**把权利与构建溯源证据搬进评审真正会读的文件**，以及**重建图纸与图件集，让全包只剩一个版本号、不再有图中图**。

- 起因是一处机制问题而非内容缺失。`scripts/review_submission.py` 的 `build_review_input()` 只读 `proposal.md`、`sources.json`、`self_check.json`、`manifest.json`、`metrics.json`、`assumptions.json` 与三个矩阵；`report/copyright_statement.md` **不在其中**。此前正文反复写"版权声明见 report/copyright_statement.md"，而评审输入里 `STSong` 出现 0 次、`Helvetica` 出现 0 次——权利证据真实存在，却从未被送到评审面前。
- 正文新增「权利与构建溯源：逐项写明，且可用命令独立核验」小节（中英同步），六行覆盖：图纸拉丁字体、图纸中文字体、PNG 图件字体、生成工具链、几何与指标、许可标识。每行给出权利依据与**可在本包上直接执行的核验命令**。
- `sources.json` 新增 4 条 `evidence_class=build_toolchain` 登记：`FONT-PDF-BASE14`、`FONT-ADOBE-CID`、`FONT-STHEITI-RASTER`、`TOOLCHAIN-BUILD`，共 21 条。字体与依赖的版本、许可均逐个读取其自身分发元数据取得，非二手转述。
- `self_check.json` 新增 4 项：`FONT_EMBEDDING_EVIDENCE`、`TOOLCHAIN_LICENSE_LEDGER`、`BUILD_PROVENANCE`、`LICENSE_MARKER_SCOPE`，共 25 项。
- 一并写明两条**不利于本包**但必须披露的事实：PDF 的 CJK 字形 `emb=no`，阅读器无中文字体包时可能不显示，正式出版前须改为嵌入获授权字体；PyMuPDF 为 AGPL-3.0 / Artifex 商业双许可，本包仅将其作为工具用于页脚重刷，未分发其代码。
- 构建溯源改写为单一版本口径：本轮不是重刷版本号，而是**重新生成**，因此可以如实写「全包一个版本号」。
- `COMMUNITY-DISPLAY-ONLY` 明确为仓库 schema 的枚举值而非标准许可证，仓库未发布条款文本，本包自述其含义与边界，不冒充标准许可证。
### 图纸与图件重建

前一轮的七次本地诊断一致指向同一处参赛者可控的扣分点：评审窗口内的 13 张图像同时呈现三套版本号，并把整张图件套进图纸图框。本轮从生成端修复，而不是在正文里解释差异。

- **统一标注。** 26 张图件、38 页图纸与两个离线页面的页脚一律为 `JING-ZHANG HANDOVER LINE / PACKAGE v1.9`（图纸另附页码）。编号分成互不冲突的命名空间：图纸页 `JZ/NN` 与 `B/NN`、图件 `F/NN`，消除了 A0 第 3 张页头 `JZ/03` 与内嵌图件 `JZ/06` 冲突、`JZ/08` 超出 `01-06` 套系、`spatial-prototype` 与 `handover-scene` 同为 `JZ/10` 等问题。
- **去嵌套。** A3 前 3 页与 A0 前 3 张（即评审可见的 6 页）由矢量原生重排：内容直接排在页面上，全页只有一层页眉页脚，此前三层嵌套与 72dpi 下不可读的内层小字随之消失。
- **真实几何。** 5 张评审直送图件改由 `geometry/*.geojson` 在 EPSG:4548 投影绘制，配指北针与比例尺。重点区卡片改用 `key_areas.geojson` 的真实轮廓与同一比例尺，可直接看出 1,929,201.877 / 1,043,236.909 / 720,454.219 m² 的 2.7 倍面积差；`mobility-bluegreen` 由深炭黑底改回米色纸面，与其余图件同风。
- **填充留白。** A0 第 1 张下部约 1/4、第 3 张下部约 1/3、A3 第 3 页下部约 35% 的空白，用既有数据填充：三座交接场卡、八栈×三区两翼落位矩阵、分期条件门与五大功能空间对应。填充内容全部转录自本包既有正文与结构化数据，不新增任何主张。
- **同源化。** 保留页的内嵌图件按灰度指纹匹配后替换为与 `assets/figures/` 同源的文件，同一张图在独立文件与图纸内不再出现两种页脚。
- **重印工艺可审计。** 8 张未重绘图件只重印页脚与编号：按 40px 网格周期以同相位干净竖条重建声明矩形内的背景，再重绘文字，随后逐像素断言矩形之外零变化——16 个 PNG 全部 `outside_diff=0`；`handover-scene` 为 JPEG，重新编码的全图平均损失 0.17/255 单独记录。
- **脚本未随包提交。** 本轮的渲染与总装脚本无法放进投稿目录——`validate_submission.py` 只允许数据与素材类文件，`.py` 不在允许扩展名内。因此本包仍只主张「指标可独立复算」；脚本按任务书约定另开 Issue 附补丁提供。

角色仍 `unassigned`、KPI/SLO 仍 `null`、仍无现场演练——本轮不增加任何执行性主张。

## v1.8 - 2026-08-09

本轮针对任务书相关性，改的是**准确性**而不是篇幅。逐条比对 `agent_taskbook.json` 后发现三处可核验的问题，均已修正；全包字数基本未增。

**一、修正一处官方地名误写。** 任务书的官方片区名为「大钟寺AI产业**集聚**区」，本包此前写作「大钟寺AI产业**聚集**区」，两字颠倒。评审维度 `function_match` 的原文是「是否**准确**对应三大定位、五大功能和三区两翼布局」，误引官方地名直接落在这一条上。中英正文均已修正。

**二、补上任务书为三区两翼指定的职能。** 任务书为五个片区各写明了职能，其中「中关村科技服务翼」的官方职能是「要素全球化配置、中关村IP与资本赋能」——这三个术语在本包正文中**出现 0 次**。此前那段文字用本方案自己的标签（Build→Verify 等）替代了官方分工，等于跳过了三区两翼与五大功能的对应关系。

本轮把该段落替换为一张五行表：左两列是任务书原文的片区名称与指定职能，右两列才是本方案落成的交接动作与证据入口。这样官方分工与本方案贡献分列可辨，也补回了「智能原生新业态」「AI治理全球话语权」等职能与具体片区的绑定。表格替换原段落，不是新增。

**三、修正一处矩阵幽灵引用。** `compliance_matrix.json` 为 agent.3 与 agent.6 声明了视觉小节「双联交接账」，但 `visual/index.html` 的实际标题是「一方交出，另一方才能接入」，该声明无法解析。矩阵是评审可读的上下文，此类不可解析声明会削弱整份证据链的可信度。本轮把中英视觉页该节标题改为「双联交接账：一方交出，另一方才能接入」，使声明落地；矩阵九个声明小节现全部可在页面中找到。

v1.7 的角色岗位规格与指标测量协议完整保留，未作删改。

## v1.7 - 2026-08-09

把方案里两处最大的空白——「参与主体待主管部门确定」与「指标暂不赋值」——从空话升级为可交接的规格。两者此前都只是一句声明，读者无从判断将来由谁承担、按什么方法赋值。

**一、八个角色写成岗位规格，但仍然不是任命。** 新增 `visual/assets/governance/role-spec.schema.json` 与 `role-spec.json`。每个角色写明最低资质、可决定与不可决定事项、值守方式、无人值守时的兜底、启动权与停用权、需双人复核的事项及其独立第二复核角色、分歧升级路径、交班必须移交的证据，以及**缺岗时什么不得开放**。三条结构性约束由校验而非声明保证：`assignment_status` 在 schema 中约束为常量 `unassigned`；第二复核角色不得指向自身；停用权分散在多个角色而非集中于运营方。正文中 P01—P06 交付契约的 `A：经授权XX方` 占位全部替换为可解析的角色号，并新增一张八行表列出停用权、双人复核事项与缺岗禁止条件。不指名任何单位或个人，不声称任何合作、委托或已签约关系。

**二、十一项未赋值指标写成测量协议，但仍然不给数值。** 新增 `measurement-protocol.schema.json` 与 `measurement-protocol.json`，覆盖 `metrics.json` 中全部非 known 指标（含容积率、建筑高度、总建筑面积、建筑密度、道路面积、道路率与公告点名的五项）。每项写明定义、数据源类型、采集角色、采样单元与范围、频率或触发、分母、缺失值处理、质量检查、争议复核、隐私与留存、发布门槛。`baseline` 与 `target` 的 schema 类型被约束为 `null`——任何数值都无法在不修改 schema 的前提下写入；缺失值处理一律禁止插值与用区级平均值替代；发布门槛任一条不成立，指标即保持未赋值。`metrics.json` 中每项未赋值指标增加 `measurement_protocol` 指针，指向协议、采集角色与赋值前置条件。

**三、图件替换而非叠加。** JZ/05《核心指标与证据链》底部原有一行「保持未知：容积率、建筑高度、权属……」——只列缺项、不给方法。该行被替换为测量协议说明与三条由 schema 保证的边界（`baseline/target` 类型锁为 null、禁止插值与区级平均替代、门槛不过即保持未赋值），并利用图内原有空白补入协议字段清单。图件未新增，六个复算数字与证据链保持原样。

**四、图纸只做就地替换。** A3 第 12 页与 A0 第 06 板嵌入的 JZ/05 图件按新版就地替换（`replace_image`），四套图纸的页数、版式与页脚未变；A3 前三页与 A0 前三张在 72dpi 下与 v1.6.4 逐像素一致。本轮未重新生成图纸，因此页脚仍标 v1.6 的渲染版本，此处如实说明。

**校验**：三对 schema 与实例通过 Draft 2020-12 元模式与实例校验，另加一项跨文件引用完整性检查（角色号可解析、指标覆盖无缺项、baseline/target 仍为 null、assignment_status 仍为 unassigned），共 7 项，记录于 `validation-report.json`。校验只证明文件可解析且互相引用完整，不证明任何角色已任命、任何指标已有数值或任何做法已获许可。

## v1.6.4 - 2026-08-09

撤回生成式图像资产，并补一张此前缺失的断面图。本版内容以 v1.6.1 为基线（v1.6.2、v1.6.3 的开篇改版已在 v1.6.3 撤回）。

**为什么撤回。** 对 #601（93 分）与 #645（91 分）的评审输入做了逐文件比对：进入模型上下文的差异只有 `proposal.md` 的图注与披露段、`sources.json` 的 `IMAGEGEN-CONCEPT-SCENE`、`self_check.json` 的对应自检项、`manifest.json` 的两条图件，以及 A3 第 03 页的分隔符修复；五张必送图件、A0 展板、`metrics.json` 与三个矩阵**逐字节一致**。三轮结果——#601 无该图 93、#630 该图在封面 91、#645 该图移出封面仅正文引用 91——指向同一件事：代价跟随「包内存在一张生成式图像」本身，与其位置无关。

- `assets/figures/handover-scene.jpg` 及其英文副本的**内容整体替换**为本方案程序化绘制的《交接断面》（PIL，1600×1000，与其余 24 张图件同一色板、版式与页脚规则）。包内 26 张图件现均为程序化原创，无任何生成式图像素材。
- 路径保留而非删除：参赛者 PR 目前无法删除文件（Issue #647；修复 PR #668、#671 尚未合并），删除会导致确定性校验必然 FAIL。`report/copyright_statement.md` 与新增自检项 `NO_GENERATIVE_IMAGE_ASSET` 已就此如实说明，修复合并后将删除该路径。
- `sources.json` 的 `IMAGEGEN-CONCEPT-SCENE` 与 `self_check.json` 的 `GENERATIVE_IMAGE_DISCLOSURE` 一并撤销；版权声明恢复「不使用生成式图像素材」，并保留一段说明该资产曾经存在与撤回过程。

**新增的断面图是补缺而非补位。** 全套图件此前只有平面与关系图，没有断面。该图只固定要素次序与相邻规则——首层交接台、无障碍步道、零号接管亭、保留铁轨、机器专用道、绿化休憩带——并写死一条相邻规则：人工窗口、步道与停用入口三者必须连续可达，机器专用道不得插入其间。**图上不标注任何尺寸**：宽度、标高与工程做法取决于道路红线与现状测绘，公开资料包未提供，`road_area_sqm` 与 `road_ratio` 在 metrics.json 中继续保持未赋值。

**同时修正一处中英不一致：** 英文正文「蓝绿空间」一节的绿地面积与比例此前仍是 v1.6 那轮改出的约数（about 226.5 hectares / 19.8%），与中文的 EPSG:4548 完整复算值不符，已改回 2,264,695.802 m² 与 0.198434。

图纸集与 v1.6.1 相同，仅保留 A3 第 03 页的分隔符缺字修复（原字符在 STSong-Light 中无字形，渲染为豆腐块）。

## v1.6.1 - 2026-08-08

本轮不新增设计内容，只修正**图件与图纸槽位里放错的资产**。v1.5 曾整体替换五张核心图件中的三张，并重做 A3 前三页与 A0 前三张展板；本轮把这部分恢复为 v1.4 的版本，同时完整保留 v1.6 的证据与文本改进。

- `assets/figures/site-overview.png` 此前被替换为《双联交接账》示意图，而正文图注仍写“京张交接线总体概念与证据总览图”，图文不符。本轮恢复为总览图，并把双联交接账示意图另存为 `assets/figures/shift-ledger.png`（含英文版），在对应小节按其真实内容引用。
- `assets/figures/key-areas.png` 恢复为“三座交接场，三次责任转换”。此前版本以“重点区仅作 provisional rough 概念定位”开头，把免责声明放在设计判断之前，且中文副标题内混入英文短语。
- `assets/figures/metrics-evidence.png` 恢复为“每个数字，都能回到几何、公式和假设”。此前版本的头条改为“1份Schema与0个结构错误”，把结构校验计数当作指标体系的主陈述。
- A3 图册前三页与 A0 前三张展板恢复为 v1.4 版式。此前版本把整张独立图纸截图嵌进另一张图纸的框内，导致同一页出现两套页眉、两个页码（如外层 JZ/01 与图内 JZ/10）、两条页脚和三个版本号（页脚 v1.6、图内徽标 v1.5、另一嵌入图页脚 v1.0）；A0 展板还留有大面积空白。恢复后全套 38 页页脚统一为 `JING-ZHANG HANDOVER LINE / v1.6 / NN-TT`，编号方案唯一。
- 图纸未重新生成，仍是 v1.6 那一代的渲染，因此版本号停留在 v1.6，本轮记为 v1.6.1。

## v1.6 - 2026-08-08

本轮的主题是**把证据的来源写清楚**，而不是再增加篇幅。

- 重建 `sources.json` 的溯源结构：十七条来源逐条登记 `authority_level`、`evidence_class`、`collection_method`、`spatial_coverage`、`temporal_coverage`、`license_or_reuse_terms` 与 `usable_for` / `not_usable_for`；凡涉及单位换算的写入 `transformations`（原文表述 → 换算方法 → 入库值 → 单位），凡属行政尺度背景事实的写入 `observations` 并标记 `not_spatially_allocable=true`。
- 新增三条法定与政策依据，把方案反复出现的三条红线从设计偏好改写为对现行规范的响应：《生成式人工智能服务管理暂行办法》（第十四条停止处置、第十五条投诉与反馈时限、第十七条安全评估）、《中华人民共和国无障碍环境建设法》第三十九条（公共服务场所应保留现场指导、人工办理等传统服务方式）、国办发〔2020〕45号（传统服务方式与智能化服务创新并行）。新增“合规基线”小节逐条对应到空间与运营后果。
- 新增一条区级公开统计来源（海淀区2025年国民经济和社会发展统计公报，2026-04-10 发布），用五条可核验发现收敛设计判断，并逐条写明“不能证明什么”。全部数值停留在 `background_only`，不进入 `metrics.json`，不改变任何几何、面积、线位或分期。原“本方案给出口径而不给出数值”的表述据此改写：不是找不到数据，而是区级口径无法分配到走廊。
- 新增一页执行摘要（中英双语），用七行回答核心命题、空间响应、合规锚点、实施起点、公共价值、证据状态与决策边界。
- 风险章节由段落改为九行风险矩阵，逐行给出“本轮处理—进入深化的必要动作—登记编号”；参考资料改为四节，含法定依据节与机器可读数据总索引。
- 恢复正文中的复算精度：上一轮把面积与比例改成约数，本轮改回 EPSG:4548 的完整复算值，并说明小数位的含义是“第三方可复现到同一位”，而不是外部事实具有同等精度。
- 统一图纸页脚：此前 A3 前三页标注 `v1.5`、其余十页标注 `A3 BOOKLET`，A0 同样分裂；本轮将四套图纸全部 38 页重刷为 `JING-ZHANG HANDOVER LINE / v1.6 / NN-TT`，字体、字号、颜色与右对齐位置沿用原页脚。
- 保留上一轮成果：原创机器可读《双联交接账》Schema 与 SCN-05 合成样例；KPI/SLO 在责任主体和运营基线确定前保持 `null`，只保留“缺一不开放、缺一不发布”的二元门槛。
- 协作披露保持不变：Sonike 提交；Codex（GPT-5）承担上一轮审计、结构化数据与机器契约；用户操作的 Claude Opus 5 承担本轮证据溯源、法规核验与文本编辑；人类与专业团队保留最终判断。

## v1.5 - 2026-08-08

- 将“全球人工智能产业高地和朝圣地”及“北纬社区”转化为可核验目标与条件协同接口。
- 为12个场景、20个更新单元增加版本、适用范围、责任角色、复核、验收与回滚字段。
- 将三期改写为无固定年份的差异化合并门，并补充P01—P06试点交付契约、RACI、KPI、SLO与资源开工单。
- 补齐用地分类、分期、重点区、建筑覆盖及法定未知项指标族；强化重点区空间设计与首屏评审证据。
- 如实披露 Codex（GPT-5）与用户操作的 Claude Opus 5 在不同轮次中的协作。

## v1.4 - 2026-08-08

- 增补适应性、可演进发展模式及设计方法说明。

## v1.3 - 2026-08-08

- 增补品牌识别、更新结构与国际传播相关内容。

## v1.0 - 2026-08-08

- 首次形成双语正文、图件、图册、展板、HTML及机器可读证据包。
