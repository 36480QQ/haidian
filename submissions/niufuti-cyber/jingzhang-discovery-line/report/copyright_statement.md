# Copyright Statement｜版权与许可声明（正式包装配版）

> 官方 scaffold 模板原文（结构起点，不直接替代本项目的逐项核验）：  
> All submitted text, geometry, diagrams, PDFs, and static HTML assets are generated by the declared AI agent or use cleared public/user-provided sources listed in sources.json. No remote assets are required by visual/index.html.

本声明按上述模板的四类对象——文本、几何、图解/PDF、静态 HTML——逐项展开来源、许可、授权状态、再分发状态和路径。本文件是供项目参与者与评审复核的正式包版权台账，不是法律意见。当前正式包已装配；“是否随包再分发”按 manifest 已登记路径记录。四份 A3/A0 中英文 PDF 已生成、进入本包并用 `pdffonts` 实测字体嵌入状态；当前 manifest 已登记并对齐本声明所列随包资产。

## 1. 登记边界与 `sources.json` 分工

- 方案实际使用的公开事实、清权资料、数据、图像、字体和工具链，应在投稿包 `sources.json` 中保存可机器追溯的来源、用途与限制。
- 字体、PDF 内置字体、Python/Node 依赖和构建工具链，应在本文件补充名称、版本或来源、许可证、是否再分发及本地资产/构建路径；若同一对象同时支撑事实或空间判断，也应在 `sources.json` 登记。
- 维护者的 `data/source_registry.json` 只覆盖仓库共享及中央复核资料。本方案的自采或自建资产不会因未进入中央表而自动获得批准，也不会仅因未进入中央表而自动被禁止。
- 外部工具生成的视觉资产在此记录工具、版本、输入来源和版权说明。生成媒体只作解释层，不替代 GeoJSON、JSON、PDF 图纸和自检结果。

以下逐项标注的留档材料可在评审阶段按要求提供。

官方合同依据为官方仓库随附的正式投稿指南、数据工作流、视觉样式建议与投稿脚手架说明；对应版本记录存项目工作区留档，未随包分发，应评审要求可出示。

## 2. 项目原创内容与一般权利状态

项目自写的中英文文本、提示词、HTML/CSS、脚本、图解编排和数据转换逻辑，由项目参与者在本项目范围内创作或组织；明确引入的第三方资料、开源组件、字体和生成式工具输出适用各自条款。投稿人已确认以 GitHub 账号 **niufuti-cyber 个人名义**署名投稿，项目原创部分按该个人署名登记；这个署名不改变第三方资料和工具输出各自的权利边界。

除下列逐项登记的内容外，本项目不主张取得第三方原始资料、开源软件、字体或平台本身的所有权。工具许可证一般约束工具软件，不当然改变用户对自有输入或自行创作文件的权利；但输出是否具备法定著作权及保护范围，仍取决于适用法律和具体的人类创作贡献。

## 3. AI 生成视觉资产（全部标注 `synthetic`）

### 3.1 共同生成与权利说明

| 项目 | 登记值 |
| --- | --- |
| 工具/模型 | 项目生成记录与批次自检登记为 OpenAI 内置 `image_gen`（GPT Image 2）；早期资产溯源记录曾记“模型名未由工具界面暴露”。本次公开口径按现有批次记录写为 OpenAI `image_gen`（GPT Image 2），不把界面未暴露过的更细模型信息写成事实。 |
| 生成时间范围 | 现有文件与批次记录覆盖 2026-08-14 至 2026-08-22（Asia/Shanghai）；页面早期候选集中生成于 2026-08-17，受控替换批生成于 2026-08-19，当前装包的场景图生成于 2026-08-22。 |
| 输入来源 | 项目自行撰写的提示词、场景合同、已登记事实边界和视觉规范；不以第三方图片作为未清权临摹输入。Logo 生成批的具体提示词存项目工作区留档，未随包分发，应评审要求可出示。 |
| 平台条款摘要 | OpenAI 面向欧洲经济区、瑞士和英国以外个人用户的 2026-01-01 生效条款写明：在用户与 OpenAI 之间、在适用法律允许范围内，用户保留输入权利并拥有输出，OpenAI 转让其对输出可能拥有的权利；用户负责确保输入和使用合法，输出可能不唯一，分享前应人工复核，且不得把 AI 输出虚假表述为人类生成。来源：`https://openai.com/policies/row-terms-of-use/`。 |
| 权利状态 | 可按平台合同在本项目中使用和分发，但不据此声明输出具有唯一性、当然取得法定著作权或已排除第三方权利风险；最终投稿仍由投稿者承担人工审查与合规责任。 |
| 随包再分发 | 仅分发最终选用或组装后的本地副本；素材池未选候选默认不进入正式包。所有随包生成图应保留 `synthetic`/“AI 生成概念图”标识和本台账指针。 |

### 3.2 页面 12 个当前 slot 资产及其源批次

当前包共有 12 个场景位：`slot-a` 至 `slot-k` 加 `slot-v15`。早期未选候选仍不随包；以下只登记当前磁盘选用件。

| 随包路径 | 当前内容／源批次 | 生成与装配方式 | 日期 |
| --- | --- | --- | --- |
| `visual/assets/slot-a.jpg` | V01；早期页面生成批 | 项目自写提示词经 OpenAI 内置 `image_gen` 生成；经静态装配链进入包，最终装配时逐字节保留 | 2026-08-17 |
| `visual/assets/slot-b.webp` | V02-B；当前场景批 | 独立 `image_gen` 调用生成，叠加 AI 概念图披露并确定性转 WebP 后选装 | 2026-08-22 |
| `visual/assets/slot-c.jpg` | V03；早期页面生成批 | 项目自写提示词经 OpenAI 内置 `image_gen` 生成；经静态装配链进入包，最终装配时逐字节保留 | 2026-08-17 |
| `visual/assets/slot-d.jpg` | V04；受控替换批 | Codex 内置 `image_gen` 生成；对源图一处 44×14 像素的伪文字斑做同材质修补，最终装配时逐字节保留 | 2026-08-19 |
| `visual/assets/slot-e.webp` | V05-A；当前修订场景批 | 以同批 V05-B 候选为构图基线做受控对象编辑，叠加披露并确定性转 WebP 后选装 | 2026-08-22 |
| `visual/assets/slot-f.webp` | V06-B；当前场景批 | 独立 `image_gen` 调用生成，叠加披露并确定性转 WebP 后选装 | 2026-08-22 |
| `visual/assets/slot-g.webp` | V07-B；当前修订场景批 | 独立 `image_gen` 调用生成 2030 年代大厅候选，叠加披露并确定性转 WebP 后选装 | 2026-08-22 |
| `visual/assets/slot-h.webp` | V08-A；当前修订场景批 | 独立 `image_gen` 调用生成，叠加披露并确定性转 WebP 后选装 | 2026-08-22 |
| `visual/assets/slot-i.webp` | V09-A；当前修订场景批 | 以本包 V01 `slot-a.jpg` 为只读同机位编辑目标，叠加“AI 生成概念图｜概念对照·非实拍”并确定性转 WebP 后选装 | 2026-08-22 |
| `visual/assets/slot-j.webp` | V10-B；当前修订场景批 | 独立 `image_gen` 调用生成，叠加披露并确定性转 WebP 后选装 | 2026-08-22 |
| `visual/assets/slot-k.jpg` | V11；受控替换批 | Codex 内置 `image_gen` 生成并以 1600×900 JPEG 装配，最终装配时逐字节保留 | 2026-08-19 |
| `visual/assets/slot-v15.webp` | V15-A；当前修订场景批 | 独立 `image_gen` 调用生成，无第三方参考图；叠加披露并确定性转 WebP 后选装；2026-08-28 保持全幅及「AI 生成概念图」水印转为 `assets/media/cover.jpg` 作封面 | 2026-08-22／2026-08-28 |

当前场景系列的逐次提示词、输入参考、通过／淘汰轮次与失败原因存项目工作区留档，未随包分发，应评审要求可出示；当前八个新增／替换场景位均已逐字节回查对应候选成品。全部随包场景图适用 §3.1，不构成实拍、现状、批准、授权、建设或运行证据。

### 3.3 招牌主标合成图（未随包）

- 名称/版本：招牌主标印位合成版。
- 来源与路径：生成场景底图来自项目 AI 场景层，项目脚本叠加项目文字、标注和 Logo；源图与清单存项目工作区留档，未随包分发，应评审要求可出示。
- 许可/再分发：AI 场景层适用 §3.1；项目标注层为自建；当前 manifest 未登记该合成图，因此它及其他候选均不随本包再分发。

### 3.4 `film-history-engineering-v1`

- 来源：GPT Image 2 生成的原创历史工程想象插图；不是历史照片，也不是逐项史实复原。
- 输入：项目自写提示词；摘要存项目工作区留档，未随包分发，应评审要求可出示。
- 路径：源图存项目工作区留档；本包登记副本为 `visual/assets/film-history-engineering-v1.webp`。
- 许可/再分发：适用 §3.1；本包登记副本已随包再分发。

### 3.5 `hero-jingzhang-atlas-v1`

- 来源：GPT Image 2 生成的概念总览；不是测绘图或工程断面。
- 输入：项目自写提示词；摘要存项目工作区留档，未随包分发，应评审要求可出示。
- 路径：源图与页面副本均存项目工作区留档，未随本包分发。
- 许可/再分发：适用 §3.1；当前 manifest 未登记该页面副本，因此不随本包再分发。

### 3.6 Logo 家族 AI 原图

| 名称 | AI 原图路径 | 后续用途 | 是否随正式包再分发 |
| --- | --- | --- | --- |
| 近山版/主标 | 项目工作区留档目录（未随包） | Inkscape/libpotrace 与 Lovart 矢量候选的来源之一 | 原始 PNG 默认不随包；组装后的纸面 lockup 已登记为 `visual/assets/brand-lockup-paper.svg` |
| 京张 | 项目工作区留档目录（未随包） | Logo 家族“京张”源图 | 原始 PNG 默认不随包；组装后的纸面 lockup 已登记为 `visual/assets/brand-lockup-paper.svg` |
| 发现线 | 项目工作区留档目录（未随包） | Logo 家族“发现线”源图 | 原始 PNG 默认不随包；组装后的纸面 lockup 已登记为 `visual/assets/brand-lockup-paper.svg` |
| 组合 | 项目工作区留档目录（未随包，含两件变体） | 组合标源图 | 原始 PNG 默认不随包；组装后的纸面 lockup 已登记为 `visual/assets/brand-lockup-paper.svg` |

这些 AI 原图由项目自写提示词通过 GPT Image 2 生成，适用 §3.1。本项目未进行 Logo 商标近似检索，也不声明生成式 Logo 当然具有唯一性、注册可用性或法定著作权保护范围。

## 4. 十对正式图（八对确定性图族＋两对恢复角色图）

- 构件核心图族八对：`key-map`、`land-use-structure`、`key-areas`、`v14-ai-on-off`、`curb-four-states`、`v16-m01-sections`、`metrics-evidence`、`task-brief-crosswalk`；每项各有中文与英文 PNG，共 16 件。
- 范围总图一对：`site-overview` 中英图由项目脚本确定性生成并恢复到官方固定角色路径；不调用生成式图像模型。页眉流程词已在重渲染时清理。
- 慢行蓝绿图一对：`mobility-bluegreen` 中英图由项目管线把九层 GeoJSON、场景合同和 UNKNOWN 声明绑定到确定性矢量事实层，并叠加明示“概念表达·合成待核”的 GPT Image 2 氛围底料；底料不承载路线、边界、比例、责任或指标事实。页眉流程词已在重渲染时清理。
- 中文构建方式：项目脚本按固定顺序确定性生成 SVG，再调用 `rsvg-convert` 栅格化并用 Pillow 做规格复核；不调用生成式图像模型，不加载远程瓦片。构建与修订记录日期为 2026-08-22，并存项目工作区留档。
- 英文构建方式：项目双语脚本从中文 SVG 的文字层生成等义英文 SVG／PNG，并把格内排版约束纳入门禁，日期为 2026-08-22。非文字 SVG 签名、数字、UNKNOWN 与边界句按审计记录保持。
- 装配后处理：隔离副本为 `key-areas` 中英图加入已批准的译题注与代码图例，只删除图面内部批次／草稿流程词并重渲 16 张 PNG；随后只归位包内路径名称，不改变图像字节。当前 16 件已逐字节回查对应 PNG。
- 数据来源：本包 `geometry/*.geojson` 九层、冻结的机制／场景合同、任务书摘录、结构化指标与本地标准摘录。九层中含 OSM 派生要素，适用 §6 的 ODbL 登记；官方／临时边界状态仍以 `sources.json` 和图面声明为准。
- 许可/归属：项目自研脚本和原创图面编排属于项目原创部分；底层数据不因此改变原许可，mobility 概念底料继续适用 §3.1 的生成视觉边界。20 张 PNG 随包再分发；SVG 与构建脚本不在当前 manifest 中，不随包。

## 5. 矢量化与图像构建工具链

| 名称/版本 | 许可证与证据 | 实际用途 | 是否随包再分发 | 本地路径/构建路径 |
| --- | --- | --- | --- | --- |
| Inkscape 1.4.4 | GNU GPL；应用内 `share/inkscape/doc/COPYING` 说明完整二进制按 GPL v3 or later 覆盖，Inkscape 官方许可页说明导出文件归作品创作者/原作者 | SVG 查询、渲染和导出；沙箱内 GUI 描摹后端失败，未把 Inkscape GUI 描摹写成已完成 | 工具不随包；其导出文件可随包 | 本机安装的 Inkscape 1.4.4；使用记录存项目工作区留档，未随包分发，应评审要求可出示 |
| Inkscape 包内 `libpotrace` 1.16 | GNU GPL；版本字符串为 `potracelib 1.16`，官方 Potrace Library API 说明按 GNU GPL 许可 | 项目编译自建 C helper，直接链接 `libpotrace.0.dylib` 逐色描摹；随后用 Inkscape CLI 查询和渲染。不是通过 Inkscape GUI 的 `object-trace` 执行 | 动态库和临时 helper 不随包；输出 SVG 是否随包取决于最终选型 | 本机安装的 Inkscape `libpotrace` 1.16；helper 与构建记录存项目工作区留档，未随包分发，应评审要求可出示 |
| Lovart 在线转换 | 投稿人已确认 Lovart 面向商用授权，本项目的 Lovart Logo 素材可商用；投稿人仍负责内容合法性，本声明不把商用授权扩张为商标注册或不侵权保证 | 投稿人在网页端手动把 Logo 位图转换为 SVG；现存三件已选矢量源与组合候选 | 最终选用的 Lovart 矢量 lockup 随包，未选候选不随包 | 投稿人确认及矢量装配记录存项目工作区留档，未随包分发，应评审要求可出示 |
| visioncortex VTracer 0.6.5 | `vtracer --version` 实测为 0.6.5；crates.io 0.6.5 元数据为 `MIT OR Apache-2.0` | 旧轮次的逐色矢量路线；已废弃，仅留档，不作为当前最终 Logo 管线 | 工具不随包；旧轮次目录默认不随正式包 | 本机安装的 visioncortex VTracer 0.6.5；旧路线档案存项目工作区留档，未随包分发 |
| librsvg / `rsvg-convert` 2.62.3 | LGPL-2.1；本机安装包所附 `COPYING.LIB` | 把构件核心图族的中英文 SVG 确定性栅格化为 16 张 PNG；把当前 PDF 构建器产生的页级 SVG 转成矢量 PDF 页 | 工具不随包；16 张构件核心图 PNG 与四份 PDF 随包 | 本机安装的 librsvg / `rsvg-convert` 2.62.3；调用记录存项目工作区留档，未随包分发 |
| Pillow 12.3.0 | MIT-CMU；本机安装包元数据 `License-Expression: MIT-CMU` 及随附许可证 | 构件核心图族使用 `PIL.Image`／`ImageFont` 做 PNG 规格、像素与文字包围盒复核；当前 PDF 构建器用 Pillow 预处理场景图并生成 PDF 内嵌 JPEG 流 | 库不随包；其输出按各资产边界随包 | 本机安装的 Pillow 12.3.0；调用记录存项目工作区留档，未随包分发 |
| Shapely 2.1.2 | BSD 3-Clause；本机安装包元数据与随附许可证，GEOS 许可另见安装包所附文件 | 上游九层 GeoJSON 生产与空间复核使用；不是当前构件核心图成图或当前 PDF 构建器的直接依赖 | 库不随包；派生几何仍按各自数据来源与 ODbL 边界登记 | 本机安装的 Shapely 2.1.2；本包派生结果见 `geometry/*.geojson` |
| pyproj 3.7.2 | MIT；本机安装包元数据 `License-Expression: MIT` 及随附许可证，PROJ 许可另见安装包所附文件 | 上游几何尺度、面积与 EPSG:4548 复核使用；不是当前构件核心图成图或当前 PDF 构建器的直接依赖 | 库不随包；复算结果不改变输入数据许可和正式／临时状态 | 本机安装的 pyproj 3.7.2；本包派生结果见 `geometry/*.geojson` |
| pypdf 6.15.0 | BSD-3-Clause；实际运行环境包元数据 `License-Expression: BSD-3-Clause` | 当前构建器合并 A3/A0 页、嵌入本地图片、写元数据并读取最终 PDF 页级结构 | 库不随包；四份 PDF 随包 | 本机安装的 pypdf 6.15.0；构建记录存项目工作区留档，未随包分发 |
| Poppler 26.04.0 CLI | GPL-2.0-only OR GPL-3.0-only；本机版本输出与 Homebrew formula 许可元数据 | 当前构建器用 `pdffonts`、`pdftotext`、`pdfimages` 校验字体嵌入、图说文字与图像对象；`pdfinfo` 作为必需的页级检查工具在开工时验依赖 | 工具不随包；只分发通过检查的四份 PDF | 本机安装的 Poppler 26.04.0 CLI；构建记录存项目工作区留档，未随包分发 |
| Python 3 标准库 | Python 运行时许可证由运行环境提供；本稿未复制解释器 | 构件核心图族、静态 HTML 派生、活地图数据抽取及当前四份 PDF 的确定性生成与检查 | 解释器不随包 | 本机安装的 Python 3；构建记录存项目工作区留档，未随包分发 |

### 5.1 活地图 JavaScript

- `visual/assets/geometry-sequence-data.js`：2026-08-20 从已验收的 16 秒活地图样机抽取九层绘制数据，写成 `window.GEOMETRY_SEQUENCE_DATA=...;`；固定图层顺序、按 `properties.id` 排序，保留 9/9 层与空 `constraints` 哨兵。数据上游仍是本包九层 GeoJSON，许可与限制沿用 §6 和 `sources.json`。
- `visual/assets/geometry-sequence.js`：从同一样机抽取 Canvas 绘制、播放控制、错误恢复与 reduced-motion 逻辑，只做本地挂载适配；项目自写，无第三方 JavaScript bundle、远程请求或 CDN。
- 两件脚本均在同一次活地图集成中新增，最终装配时逐字节保留；工具与运行逻辑不改变临时边界、OSM 或项目自建数据的既有权利状态。

### 5.2 四份当前 PDF 的构建溯源

- 路径：`drawings/a0-boards.pdf`、`drawings/a0-boards.en.pdf`、`drawings/a3-booklet.pdf`、`drawings/a3-booklet.en.pdf`。
- 当前生成日期：2026-08-28。唯一当前构建入口为项目确定性 PDF 构建脚本；脚本与运行记录存项目工作区留档，未随包分发，应评审要求可出示。
- 输入：20 张数据图、12 张当前场景资产、两份 proposal 中的图说／风险段和两份 visual HTML 中的场景图说。脚本不读取远程资源，也不使用旧版图族构建产物。
- 生成方式：先重建中英 A3 并保持 14 页结构，再按「总体设计结构→用地→三重点区→交通蓝绿→AI 开关→路缘四态→签名栏诚实板→任务书对照」重建中英 A0。脚本确定性生成页级 SVG，使用 `rsvg-convert` 生成矢量 PDF 页，用 Pillow 生成本地嵌入 JPEG 流，再由 pypdf 合并。内嵌图品质沿用 PNG 74／其他图像 64。
- 图面来源条已改用公开标签，内容与几何未变。
- 内置门禁：页数、纸张尺寸、图说与场景披露、板序、禁止流程词、字体嵌入和页内图像对象均须通过；四件当前产物另逐份用 `pdffonts` 复核，全部字体对象 `emb=yes`。这只证明技术嵌入，不等于字体许可已经独立清理。

## 6. 数据与征集方资料

### 6.1 OpenStreetMap

- 名称/来源：OpenStreetMap data，`© OpenStreetMap contributors`。
- 许可证：Open Data Commons Open Database License 1.0（ODbL 1.0）。官方说明允许复制、传播和改编，但须署名 OpenStreetMap 及其贡献者；改编或建立于其上的数据库分发时适用相同许可。来源：`https://www.openstreetmap.org/copyright`。
- 本地记录：原始分析底图存项目工作区留档，未随包分发；派生要素进入本包 `geometry/` 九层中的相关文件。
- 是否随包再分发：当前 manifest 登记的 OSM 派生几何和使用该数据的静态图随本包再分发。本项目尚未对随包数据集做 Derivative Database 的法律分类，因此不声明该分类已解决；所有相关图面、PDF/HTML 和 `sources.json` 按 ODbL 边界保留 `© OpenStreetMap contributors` 与 ODbL 1.0 指针，若实际分发构成 Derivative Database，则按 ODbL 同许可条件提供对应数据库。
- 静态页内嵌的 119 条 OSM 走廊登记表属 ODbL 数据再分发形态，署名与限制同本节。

### 6.2 官方 site-package 与征集资料

- 名称/来源：征集方/官方仓提供的 `brief/site-package/`、公开公告及任务书摘录。
- 授权状态：仓内 `brief/site-package/sources.json` 将任务书摘录标为 `User-provided cleared document` / `user_provided_cleared`，公开公告按官方来源登记；临时边界只可用于临时生成、离线可视化和讨论，不是官方红线或审批依据。
- 许可证：官方仓根目录没有发现统一 LICENSE 文件；公开事实可在本征集方案中引用不等于原文件获得通用再许可。本包不整仓复制官方 site-package，只使用征集必需的结构化摘录、来源 ID 和允许使用的几何，不对官方原文件作通用再分发许可声明。
- 是否随包再分发：投稿包只携带赛事要求的必要结构化摘录、引用 ID 或经允许的几何，不默认复制整个官方仓；官方仓库副本存项目工作区留档，未随包分发，正式使用项应逐项进入 `sources.json`。

## 7. 字体与 PDF 嵌入

### 7.1 macOS 系统字体

| 字体 | 用途 | 是否随包分发 | PDF 子集嵌入 |
| --- | --- | --- | --- |
| Songti SC / STSong | 页面与十对正式图中文标题／宋体回退 | 不复制字体文件，不随 HTML 包分发 | 四份 PDF 实测命中 `STSongti-SC-Bold`，`emb=yes`、`sub=yes` |
| PingFang SC | 页面中文正文/系统回退 | 不复制字体文件，不随 HTML 包分发 | 四份 PDF 未实测命中 PingFang；实际中文无衬线回退命中 `HiraginoSansGB-W3/W6`，`emb=yes`、`sub=yes` |
| STHeiti（若最终 CSS/PDF 实际命中） | 系统黑体回退 | 不复制字体文件，不随 HTML 包分发 | 当前静态页没有显式指定 STHeiti，四份 PDF 也未实测命中 STHeiti |

四份当前 PDF 于 2026-08-22 重导后再次逐份运行 `pdffonts`；所有字体对象均为 `emb=yes`，常规字体为子集嵌入，部分由 SVG 转换产生的 Type 3 自定义字形为 `emb=yes`、`sub=no`。当前构建器只在构建时检查，不把完整工具原始输出写入正式包。这只证明技术嵌入状态，不证明许可成立。Apple macOS 软件许可的字体条款允许在 Apple 软件运行时显示和打印内容，但嵌入仍以该字体的 Font Book/Preview/Show Font Info 所列限制为准；因此本稿不把“PDF 字体嵌入已获许可”写成既成事实，也不单独分发字体文件。

### 7.2 Barlow Condensed 随包字体文件

- 当前状态：该字体目录属于包外交互原型，不在本 70 件正式包内；虽然 V2 源 CSS 已改为系统字体且没有发现 `@font-face` 或 Barlow 引用，但 `public/assets/fonts/` 和 `dist/client/assets/fonts/` 当前仍各有 400、500、600、700、800、900 六个 TTF 副本；因此不能声明字体已清空。
- 名称/版本：Barlow Condensed，字体内部 `fontversion=92274`；六个权重分别对应 Regular、Medium、SemiBold、Bold、ExtraBold、Black。
- 许可证：SIL Open Font License 1.1（OFL-1.1）；上游 Barlow 项目与 Google Fonts `ofl/barlowcondensed/` 公开登记该许可。
- 明确披露：该字体目录属于包外交互原型，不在本 70 件正式包内；交互原型的 `public/` 与 `dist/` 仍各有 6 个 Barlow Condensed TTF，当前静态投稿页不携带也不引用这些字体。本次已装配静态包不纳入交互原型的未引用字体目录；若后续改为分发该 bundle，必须随包提供 OFL 正文与来源记录。
- 是否随包再分发：本次静态包不纳入 V2 的未引用字体目录；若后续改为分发 V2 bundle，必须随包提供 OFL 正文与来源记录。

### 7.3 Source Han Serif SC Heavy（Logo 历史构建输入）

- 名称/来源：Source Han Serif SC Heavy；项目已存字体及 `LICENSE-OFL-1.1.txt`。
- 许可证：SIL Open Font License 1.1。
- 用途：部分 Logo/招牌图标注层把字形转换为 SVG path；最终页面不依赖该字体文件运行。
- 路径：字体文件与 `LICENSE-OFL-1.1.txt` 存项目工作区留档，未随包分发，应评审要求可出示。
- 是否随包再分发：字体文件默认不随正式包；已转轮廓的项目图形按 OFL 和项目原创编排登记后随选用资产分发。

## 8. Node 直接依赖

交互原型的 `package.json` 与实际解析到的安装包共同用于核实下表版本和许可。依赖源码与完整 `node_modules` 不随本次静态包分发；当前 `visual/index.html` 只含项目自写内联脚本，不引用该第三方 JS bundle，因此本次静态包不产生传递依赖 notice 缺口。若后续改为分发该 bundle，下表 MIT 组件及实际进入 bundle 的传递依赖必须随包保留对应版权和许可通知。

| 直接依赖 | 实装版本 | 许可证 | 版本/许可证据路径 | 再分发状态 |
| --- | --- | --- | --- | --- |
| `@phosphor-icons/react` | 2.1.10 | MIT | 本机安装包元数据与随附 `LICENSE` | 图标相关构建代码可能进入 bundle；包源码不整体分发 |
| `@vitejs/plugin-react` | 5.0.4 | MIT | 本机安装包元数据与随附 `LICENSE` | 仅构建期，不随运行包 |
| `react` | 19.2.0 | MIT | 本机安装包元数据与随附 `LICENSE` | 运行代码进入交互式 bundle；若最终静态页不含该 bundle，则不随静态包 |
| `react-dom` | 19.2.0 | MIT | 本机安装包元数据与随附 `LICENSE` | 同上 |
| `vite` | 6.4.2 | MIT | 本机安装包元数据与随附 `LICENSE.md` | 构建期；Vite 客户端辅助代码是否进入 bundle 以最终产物为准 |

直接依赖清单来自项目工作区留档的交互原型 `package.json`，未随包分发，应评审要求可出示。传递依赖不在本声明逐项展开，因为当前随包静态页不携带该 bundle；如果后续装入 JS bundle，需用构建时 license inventory 生成与实际随包代码一致的传递依赖通知。

### 2026-08-27 增补说明

四个新增章节、119 条 OSM 登记、5 项离线演练指标、4 个治理文件已分别由 §9 覆盖矩阵与 `sources.json` 登记承接。

## 9. 正式包逐件覆盖矩阵

本表按当前 71 件实物建立互斥路径族；每一族同时指向本声明的版权段和 `sources.json` 的来源／豁免记录。项目原创、自说明元数据和从同包主稿确定性派生的 HTML 不需要虚构外部来源，但必须落入明确的项目原创或自说明豁免。

| 覆盖 ID | 当前路径（逐件或互斥路径族） | 件数 | 本声明位置 | `sources.json` 记录 |
| --- | --- | ---: | --- | --- |
| `COPY-COV-01` | `agent.json`、`assumptions.json`、`compliance_matrix.json`、`design_depth_matrix.json`、`manifest.json`、`metrics.json`、`self_check.json`、`sources.json`、`standard_matrix.json` | 9 | §2；其中 manifest／self_check／sources 为自说明账 | `PACKAGE-PROJECT-ORIGINAL-20260822`、`PACKAGE-SELF-DOCUMENTING-METADATA-20260822` |
| `COPY-COV-02` | `proposal.md`、`proposal.en.md`、`report/proposal.html`、`report/proposal.en.html`、`report/copyright_statement.md`、`visual/index.html`、`visual/index.en.html` | 7 | §2、§8、§9 | `PACKAGE-PROJECT-ORIGINAL-20260822`、`PACKAGE-HTML-ASSEMBLY-20260822`、`PACKAGE-SELF-DOCUMENTING-METADATA-20260822`、`OSM-CORRIDOR-REGISTRY-20260827` |
| `COPY-COV-03` | `assets/figures/*.png`（中英 10 对） | 20 | §3.1、§4、§5 | `ASSET-FIGURE-FAMILY-20260822`、`ASSET-SITE-OVERVIEW-20260818`、`ASSET-MOBILITY-BLUEGREEN-20260821`、`RIGHTS-GPT-IMAGE-2026` |
| `COPY-COV-04` | `drawings/*.pdf` | 4 | §5.2、§7 | `BUILD-CURRENT-PDF-20260822` |
| `COPY-COV-05` | `geometry/*.geojson` | 9 | §4、§6 | `DATA-SRC-PROVISIONAL-BOUNDARIES-20260605`、`OSM-2026-OVERPASS-SNAPSHOT`、`PACKAGE-PROJECT-ORIGINAL-20260822` |
| `COPY-COV-06` | `visual/assets/slot-*`（A—K 加 V15） | 12 | §3.2 | `ASSET-PAGE-SCENE-RETAINED-20260817-19`、`ASSET-SCENE-FAMILY-20260822` |
| `COPY-COV-07` | `visual/assets/brand-lockup-paper.svg`、`visual/assets/film-history-engineering-v1.webp`、`visual/assets/geometry-sequence-data.js`、`visual/assets/geometry-sequence.js` | 4 | §3.4、§3.6、§5.1 | `RIGHTS-LOVART-2026`、`RIGHTS-GPT-IMAGE-2026`、`ASSET-GEOMETRY-SEQUENCE-20260820` |
| `COPY-COV-08` | `report/narrative.md` | 1 | §2、§9 | 项目自说明的留档目录／指纹索引；不复制外部或留档原文，不新增外部来源 |
| `COPY-COV-09` | `changelog.md`、`risk.json`、`simulation.json`、`spatial.json` | 4 | §2 | `PACKAGE-PROJECT-ORIGINAL-20260822`、`PACKAGE-SELF-DOCUMENTING-METADATA-20260822` |
| `COPY-COV-10` | `assets/media/cover.jpg` | 1 | §3.2 | `ASSET-SCENE-FAMILY-20260822`、`RIGHTS-GPT-IMAGE-2026` |
|  | **合计** | **71** | 71／71 有版权路径 | 71／71 有来源或明确豁免路径 |

本矩阵的路径族只覆盖当前实物，不豁免未来新增文件。正式包若增加或删除文件，必须同步更新本表与 `sources.json.package_asset_coverage` 后重新计算覆盖。

## 10. 非公开资料、隐私、AI 责任与状态声明

1. **非公开资料排除**：本包不纳入未获明确授权的非公开规划资料、内部运营数据、个人订单/私信、账号凭证、Cookie、token、数据库或登录态。此类内容已从正式包排除；如后续发现，应移除并记录来源缺口。
2. **隐私保护**：现有视觉资产不包含项目已知的可识别真人个人信息；图中人物为合成叙事角色，不是调研样本、真实个人画像或客流证据。最终包仍应对图像、文本、元数据和 PDF 附件做一次隐私复核。
3. **AI 生成责任**：所有 AI 图标注 `synthetic`/“AI 生成概念图”；投稿者负责核对事实、权利、准确性、适当性和潜在近似，不把生成图当作测绘、工程或现场证据，也不把 AI 输出冒充纯人工生成。
4. **不构成批准或实施承诺**：本方案、图件、Logo、数据推演和场景卡均不表示征集方、政府部门、产权方、运营方或其他机构已批准、授权、采用、建设或实施。临时边界、候选数据和未知项按原状态显示。
5. **离线资产**：当前 `visual/index.html` 不依赖 CDN、远程地图瓦片、远程字体、iframe、表单、外部 API 或跟踪代码；官方 `visual_review.py` 与剥脚本副本结果已验证，记录存项目工作区留档，未随包分发，应评审要求可出示。当前 manifest 已登记该静态页及随包本地资产，并已完成路径与哈希对齐；这不改变任何版权、许可或再分发边界。

## 11. 十一项结论与诚实披露

1. **署名已关闭**：投稿与项目原创内容以 GitHub 账号 **niufuti-cyber 个人名义**署名。
2. **AI 公开口径已关闭**：投稿人已确认同意公开披露本项目使用 AI 工具；公开口径为“项目批次记录使用 OpenAI `image_gen`（GPT Image 2）生成概念视觉，由人工负责事实、权利和选用”；生成日期按现有批次记录披露到日。
3. **随包资产边界已写明**：当前包分发 12 张场景 slot、`film-history-engineering-v1.webp`、组装后的 `brand-lockup-paper.svg`、中英双语十对正式图 PNG、两件活地图 JavaScript 与四份当前 PDF；招牌主标合成图、`hero-jingzhang-atlas-v1`、AI Logo 原图及未选候选不随包分发。
4. **Logo 权利边界诚实披露**：本项目未完成商标近似检索，不声明 Logo 已可注册、独占或当然受到某一范围的法定保护。
5. **Lovart 商用边界已披露**：投稿人已确认本项目 Lovart Logo 素材可商用；本轮未独立核验平台授权条款，这一投稿人确认仍不替代商标近似或第三方权利检查。
6. **OSM 分类未被伪造关闭**：本项目未对随包数据做 Derivative Database 法律分类；当前按 ODbL 保留署名、许可指针和必要时的同许可数据库提供义务，不声称分类已完成。
7. **官方资料再分发边界诚实披露**：官方仓未见根级统一 LICENSE；投稿包不整仓复制，只携带征集必需的结构化摘录、来源 ID 和允许使用的几何，不对官方原文件作通用再许可声明。
8. **PDF 字体现状诚实披露**：四份 A3/A0 中英文 PDF 已生成并逐份用 `pdffonts` 实测；全部字体对象 `emb=yes`，常规字体 `sub=yes`，少量 Type 3 自定义字形 `sub=no`。技术嵌入不等于许可，本声明不写成已获嵌入许可，字体文件也不单独分发。
9. **Barlow 处置已明确**：V2 `public/` 和 `dist/` 仍有未引用的 Barlow Condensed TTF；本次静态包不携带该字体目录。若改为分发 V2 bundle，必须同时携带 OFL 正文和来源记录。
10. **JS 依赖边界已明确**：当前静态页只含项目自写内联脚本，不携带 V2 第三方 bundle，因而本次静态包没有传递依赖 notice 缺口；若以后装入 bundle，必须以实际随包代码生成 license inventory。
11. **离线页面与 manifest 已核**：当前 `visual/index.html` 的零远程依赖与本地资产存在性已经实跑核验，记录存项目工作区留档，未随包分发，应评审要求可出示；当前 manifest 已登记并对齐该页面及本地资产的路径与哈希。这只证明本地装配一致，不表示版权许可、对外提交、受理、入选或实施已经完成。
