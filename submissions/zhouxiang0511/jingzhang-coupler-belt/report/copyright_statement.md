# 版权声明 / Copyright Statement

## 中文

1. 本方案全部提交内容（正文、GeoJSON 几何、指标、图纸、PDF、HTML 展示页与生成图件）由声明的 AI Agent（DeepSeek V4 Flash harness agent）生成，或使用 `sources.json` 中登记的清权/公开来源。
2. 方案文本与图件版权责任由提交者（GitHub 用户 zhouxiang0511）承担；AI 生成内容不主张独立版权，作者对事实、引用、版权与最终表达负责。
3. 图件全部由提交者从自身提交的 GeoJSON 与指标数据派生生成，不含未授权商标、字体、图片、人物肖像或论文图像。Logo 与地标设计仅提供方向性概念，不包含任何受保护标识。
4. 边界几何使用仓库维护者提供的临时边界（provisional boundaries, `official_boundary=false`），版权与许可归属原始来源（官方公告文字依据），仅作概念展示，不作为官方红线。
5. 背景核对数据（如有 OSM 派生统计）仅发布统计读数并保留 ODbL 1.0 署名与使用边界，不再分发原始数据。
6. `visual/index.html`、`visual/index.en.html` 与 `report/proposal.html` 为完全离线静态页面，不加载任何远程资源。
7. 本方案以 "COMMUNITY-DISPLAY-ONLY" 许可在公开仓库展示；若后续被选中深化，作者同意按项目公开征集规则授权使用。

## English

1. All submitted artifacts (proposal text, GeoJSON geometry, metrics, drawings, PDFs, HTML pages and generated figures) are produced by the declared AI agent (DeepSeek V4 Flash harness agent) or use the cleared/public sources registered in `sources.json`.
2. Responsibility for facts, citations, copyright and final expression rests with the submitter (GitHub user zhouxiang0511); AI-generated content does not assert an independent copyright.
3. All figures are derived by the submitter from the submitted GeoJSON and metrics; they contain no unauthorized trademarks, fonts, images, portraits, or paper figures. Logo and landmark designs are directional concepts only.
4. Boundary geometry uses the repository-maintained provisional boundaries (`official_boundary=false`), whose rights and licensing follow their original sources (official announcement text); they are used for conceptual display only and are not official redlines.
5. Any OSM-derived background statistics (if used) publish aggregate figures only, retaining ODbL 1.0 attribution and use boundaries without redistributing raw data.
6. `visual/index.html`, `visual/index.en.html` and `report/proposal.html` are fully offline static pages with no remote resources.
7. This proposal is displayed in the public repository under the "COMMUNITY-DISPLAY-ONLY" license; if selected for deepening, the author agrees to licensing per the project's public open-call rules.
