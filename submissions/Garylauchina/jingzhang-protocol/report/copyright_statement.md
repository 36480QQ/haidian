# Copyright Statement

All submitted text, concept geometry, analytical diagrams, PDFs, and static HTML assets are generated by the declared AI agent or use cleared public/user-provided sources listed in `sources.json`.

The twelve analytical PNGs in `assets/figures/` are generated locally from the submission's own `geometry/*.geojson`, `metrics.json`, `assumptions.json`, registered sources, evidence matrices, and self-check state. Four Chinese/English pairs retain their truthful V0.7 evidence-snapshot footer because their underlying coordinates and derived spatial metrics did not change; the FP01 100-day contract pair is V0.8. The V0.9 `site-overview` pair is a new deterministic diagram of the six public application capabilities, the trusted-operations layer, known structure versus unknown delivery performance, and the provisional spatial mapping. It uses no external basemap, policy screenshot, flag, government emblem, photograph, logo, portrait, remote font, or network image. System fonts are referenced for rendering but are not redistributed.

The four V0.8 conceptual-experience PNGs in `assets/media/` were generated through OpenAI's built-in image-generation service from original project prompts and without an input/reference image. They contain no requested logo, trademark, identifiable real building or copied photograph. C2PA provenance metadata is retained in the submitted PNGs. `cover.png` is a composite conceptual corridor view; the three `scene-*.png` files are design-intent illustrations, not site photographs, surveyed existing conditions, precise siting, approved architecture or implementation evidence.

The four V0.9 drawing PDFs in `drawings/` are generated locally from the paired proposal text, the twelve analytical PNGs, the four conceptual-experience PNGs, and the submitted structured evidence. The Chinese and English A3 booklets each contain twelve pages; the Chinese and English A0 sets each contain three boards. They use no external basemap, photograph, logo, portrait, remote image, or network resource. The PDFs embed document subsets of locally installed macOS fonts for portable display; no standalone font file is included. Font embedding is disclosed for repository and publication-rights review.

The paired V0.9 `visual/index.html`, `visual/index.en.html`, `report/proposal.html`, and `report/proposal.en.html` files are offline static artifacts. They load no remote script, stylesheet, map tile, font, iframe, form, tracker, or external API.

The railway history, national AI-policy context, Beijing/Haidian project context, and global comparison cases are paraphrased from the registered public sources; no source is reproduced in full. The strategic framing is a proposal interpretation and does not use official visual identity or imply national-pilot designation, government endorsement, approved planning status, adoption, delivered performance, or proven world leadership. The provisional geometry and every geometry-derived metric remain concept evidence, not an official planning or engineering record.
