# Copyright and Provenance Statement / 版权与来源声明

- 文字、结构化数据和概念设计 GeoJSON 由 `zyaoii × OpenAI Codex` 生成；空间数据依据、用途与限制见 `sources.json` 和 `assumptions.json`。
- 仓库 `brief/site-package/geometry/provisional_boundaries.geojson` 仅作为临时粗略协调底板；没有商业地图截图、远程地图瓦片、未授权规划图或非公开资料。
- `assets/media/cover.webp` 由 Codex 内置 image generation 在无输入图、无参考图、无第三方视觉资产条件下生成；完整 prompt 与限制见 `assets/media/cover-provenance.md`。它只表达 sealed heat / straight noise path / staggered porosity+shade+quiet-side 的 E0 空间假设，必须标注“AI-generated conceptual illustration / AI生成概念示意，不作为场地证据”。不得从像素推导真实场地、几何、尺寸、仿真、施工、批准或绩效。
- Ladybug Tools、OpenFOAM、NoiseModelling 与 pythermalcomfort 只作为公开方法库登记；本包未复制其代码或图形，未运行场地模型，也未移植默认参数或结论。具体许可证与用途限制见 `sources.json`。
- HTML、图件和 PDF 由并行视觉流程另行生成和核验；本数据构建器不修改这些文件。任何正式发布前须重新检查字体、图标、生成媒体、依赖许可证、双语副本及 manifest 哈希。

All generated media are explanatory. JSON/GeoJSON, evidence registries and qualified human review remain authoritative.
