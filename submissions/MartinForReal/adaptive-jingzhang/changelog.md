# 方案迭代记录

## v0.1 - 2026-08-14

### 本地审批检查点

当前状态：**等待用户明确批准后才进入实现。**

本检查点仅提交 `regeneration-design.md` 与本文件，供人审阅完整再生架构。由于 `regeneration-design.md` 不属于最终封闭包语法，本地审批提交有意不具备最终 CI 条件；在批准前不实现、不推送、不新建替代 PR，也不改动 GitHub。

### 已完成：方案、Dynamic Workflow 与独立审计

- [x] 使用本机 Claude Code、Opus 5 与 Dynamic Workflow 对现有方案、城市设计逻辑、复杂适应系统、证据边界、验证器约束和发布流程完成全量审阅。
- [x] 将分散的模型输出合并为单一、可审阅的完整再生规范，并独立校正其内部矛盾。
- [x] 冻结中英文标题、包状态、临时边界属性、四个自检 ID、七项人工门、场景/项目/人物注册表契约。
- [x] 将计算方法统一为 seeded minimum-spanning-tree topology/disagreement probe，并明确南北连通由生成规则构造，不能作为城市需求或廊道发现。
- [x] 锁定 10 个节点、24 条候选边、64 次运行、704 个选择令牌、22 条曾选边、E06/E24 未选择、阈值敏感性与三项关键派生值。
- [x] 明确保留两份 Physarum 数据快照，只允许对复算器进行两项验证比较的有限修订；篡改测试仅使用临时副本。
- [x] 将设计主线收敛为“Four Working Rooms, Three Decision Seams”临时假设、三个不同的城市学习实验室，以及非官定的京张气候—记忆公共空间系统。
- [x] 补齐三个重点区的差异化用户、空间界面、流线、无障碍链、季节/中断运行、治理、维护、停止与恢复契约。
- [x] 固定 30 个新增双语重点区图版、现有双语总览图、A3/A0 编排、56 个最终新增文件及 manifest 声明规则。
- [x] 固定 13+13 个报告章节、23 个合规任务、15 个设计深度 ID、5+5 个必需图件、9 个根 JSON、9 个 GeoJSON 与 19 个现有指标。
- [x] 通过再生规范专用机械审计，并确认 UTF-8 无 BOM、仅 LF、低于 256 KiB、无工作流 ID、无绝对机器路径、无未完成作者标记。
- [x] 更新本 TODO 账本，严格分开已完成审阅、未开始实现与外部专业依赖。

### 待办：批准后的实现任务

- [ ] 获得用户对 `regeneration-design.md` 所列核心决策的明确批准。
- [ ] 调用 writing-plan 工作流，将已批准规范分解为逐文件实现计划，不新增未批准路径。
- [ ] 先编写失败的标准库 JavaScript 契约测试，再开始修改生成器、其他结构化数据、叙事、图件、HTML 与 PDF；两份冻结的 Physarum 数据快照除外。
- [ ] 修正所有英文标题中的多余 “The”，并保持中英文标题、章节、数值、状态和语义块完全对应。
- [ ] 建立双语源数据、场景/项目/人物/门/角色/状态变量/图件/声明限制/指标映射等注册表。
- [ ] 实现 Zhongzhiyuan 控制验证公地、北京 AI 原点社区校园—社区门槛、Dazhongsi 到达—停留—修复三套不可互换的详细设计。
- [ ] 生成 30 个新增双语重点区图版，更新既有 10 个必需图件，并重建四份嵌入字体且可提取文本的双语 PDF。
- [ ] 实现连续无障碍路线链、完整季节/中断矩阵、公共空间六种日常市民场景、治理/维护/停止/恢复记录。
- [ ] 保持 `physarum-inputs.json` 与 `physarum-runs.json` 字节不变；有限修订 `reproduce_physarum.js`，达到 `PASS / 633 comparisons / 7/7 / 0 mismatches`。
- [ ] 实现零抖动消融、七项字段篡改测试、双清洁构建树哈希一致性、陈旧/未声明输出故障注入测试。
- [ ] 完成双语、离线、键盘、无障碍、版权、字体、PDF 结构、渲染器、尺寸、哈希与声明范围验证。
- [ ] 将所有新增文件先写入 manifest 条目，再刷新 manifest；随后运行标记自检，且不得在标记自检后再次刷新。
- [ ] 将批准内容迁移到允许的最终包文件，并在最终预检前删除审批专用 `regeneration-design.md`。
- [ ] 运行完整本地命令链、`git diff --check`、包内范围检查和 participant preflight，修复全部错误。
- [ ] 在独立分支推送，创建新的替代 PR，并持续监控 CI、审阅意见与合并状态直至明确终局。

### 外部数据与专业触发器（不是实现勾选项）

以下事项不能由模型、实现提交或 TODO 勾选来伪造完成；只有相应机构或专业人员提供证据后才触发复算与再生成。

| 外部触发器 | 责任方 | 触发后的主要动作 |
|---|---|---|
| 正式场地边界或重点区边界发布 | 规划主管部门 | 复算面积、绿地比例、重点区关系并再生成全部相关图件、指标与报告 |
| Dazhongsi 站点接口的清晰正式证据发布 | 轨道/站点主管部门 | 重新校核 2.26 km 位置差异问题并全面重做 Lab 3 接口 |
| 地籍、权属、通行权或维护责任资料取得 | 土地管理及相关权利方 | 复核所有条件性首层、界面和治理安排 |
| 地形、标高与路线测量完成 | 持证测量人员 | 复核所有剖面、坡度、无障碍路线与局部基准 |
| 适用技术规范正式登记 | 规范登记责任方与专业团队 | 将可证实尺寸从概念模块迁移为规范来源尺寸 |
| 无障碍、交通、结构、市政、遗产、消防、隐私等专业审查完成 | 对应持证专业人员或主管机构 | 更新相应 G1–G7 状态、停止阈值、方案与责任记录 |
| 运维、清洁、除雪、修复及恢复资金落实 | 实施/运营主体 | 更新 G4、维护责任、预算所有者与部署许可 |
| 至少一个夏季高温/降雨周期及一个冬季冰雪周期完成观察 | 具名运营者与批准角色 | 决定继续、修改、停止、恢复或跨尺度传播 |

### 审批后第一条不可变规则

在用户批准前，本账本中的所有实现项保持未勾选；任何外部依赖均不得改写为已完成设计工作。
