# 版权、来源与生成说明 / Copyright, Sources, and Generation

- 本包候选专属正文、协议/任务/fixture JSON、HTML、十一组双语图件和双语 PDF 由京张贡献执行员在本轮原创生成。 / Candidate-specific narrative, protocol/task/fixture JSON, HTML, eleven bilingual figure pairs, and bilingual PDFs were authored in this pass.
- 通用 schema 完整结构和拓扑安全几何方法复用作者本人已合并 `jingzhang-rights-handover` 包；所有公共任务、生命周期、角色、标签和表达均重写。 / Schema-complete structure and topology-safe geometry methods reuse the author's own merged package; public task, lifecycle, roles, labels, and expression are rewritten.
- PNG 与高分辨率 A3/A0 页面由 Pillow 从包内数据确定性绘制；`visual/assets/verify-fixtures.js` 逐项验证 60 条 fixture 的预期/实际值和四项失败分支。这些是合成设计验证，不是现场结果。 / PNG and high-resolution A3/A0 pages are deterministically rendered with Pillow from package data; `visual/assets/verify-fixtures.js` verifies expected/actual values plus failure branches for all 60 fixtures. This is synthetic design verification, not a field result.
- Noto Sans CJK 使用 SIL OFL 1.1，DejaVu Sans 使用 DejaVu Fonts License；均为系统字体，不随包再分发。 / System fonts are not redistributed.
- `imagegen` built-in generator was unavailable; no API-key fallback was used, and `manifest.cover_image` remains null for the website default cover. The twelve added task boards are deterministic Pillow diagrams, not generated site renderings.
- 未嵌入第三方图片、地图瓦片、Logo、人物、声音、视频、远程字体/脚本、个人数据或非公开空间数据。 / No third-party images, map tiles, logos, people, audio, video, remote fonts/scripts, personal data, or non-public spatial data are embedded.
- 国际案例只链接并摘要问题，不复制媒体或长段文本；用途和限制见 `sources.json`。 / International cases are linked as background questions only; see `sources.json`.
